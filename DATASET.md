# jianpu2 语料说明（数据集卡）

简谱（数字谱）扫描图 → 文本 token 序列的自动转写语料。
自动生成，**未人工校对**（除 `train-work/gt/` 的少量 GT 用于评测）。

> **当前快照（2026-09-22 凌晨，以此为准）**
> 语料 **7107 份**（`batch-out`，约 120 万音符）；成品曲谱 `jianpu-db-out/scores/` **6601 份**
> （PNG 补转收尾后约 7000），**100% 带 `source=` 出处**（站点 + 站点 id，可回溯原页）。
> 来源分布：qupu123 3071 / jianpu.cn 2982 / jianpujia 544 / 手工抓取 4。
> 最新数字见 `train-work/DELIVERY.md`（自动生成）与 `py -3.13 tools/verify_deliverable.py`（7 项总验收，当前全过）；
> 能力指标与口径见 `SKILL.md`。
> **下面各节的方法说明仍然有效，但其中的数量是当时的历史快照。**

---

## 一、数据从哪来

| 源 | 站点 | 站点定位 | 成品曲谱数 |
|---|---|---|---|
| `qupu123` | 曲谱网 | 简谱/五线谱/正谱/双谱，民歌红歌教材为主 | 3071 |
| `jianpujia` | 简朴家 | 儿歌、教材歌曲 | 544 |
| `jianpu.cn` | 歌谱简谱网 | **流行歌为主** | 2982 |
| 手工抓取 | hot-crawl | 单曲补抓（`source=unknown` / 站名） | 4 |

> 数字为**成品曲谱**（`jianpu-db-out/scores/`）的 2026-09-22 快照；语料数比它略多
> （转写成功但被择优/隔离的部分）。

**为什么补 jianpu.cn**：qupu123/jianpujia 不收港台流行。jianpu.cn 有陈奕迅/罗大佑/
梁静茹/莫文蔚/孟庭苇/张靓颖/光良/张敬轩/林忆莲/李宗盛/黄品源等约 40 位歌手的谱。

---

## 二、目录结构

```
images-prep/                原图（按源分目录，每个目录一首）
  ├─ jianpucn-pop/          jianpu.cn 爬来的（流行）
  ├─ qupu123-crawl/
  ├─ jianpujia-crawl/
  └─ hot-crawl/
batch-out/                  转写结果：<目录名>.txt（token 序列）+ .png（标注框图）
batch-out-bad/              隔离：非纯简谱（五线谱/六线谱/总谱/吉他混合谱）
batch-out-dup/              隔离：重复谱 —— ①同名歌的落选版本（`pick_best`）②**整串旋律完全相同但曲名不同**的（`dedupe_by_content`）
batch-out-empty/ -suspect/  隔离：0 音符的 / 质量可疑的（高念白率）
jianpu-db-out/scores/       jianpu-db 格式曲谱（**交付物**）
jianpu-db-out/scores-prev/  上一版 scores（**不删**：重建时复用拍号 + 继承人工 `todo=`/`preferred=`）
train-work/jpdbtest/out.jsonl  HF 格式 JSONL（交付物，副本产出，未动用户仓库）
train-work/source_map.tsv   标题 -> 来源站 -> 目录名（**溯源用**）；`source_map.html` 带检索链接可点开核对
train-work/title_clean.tsv  文本模型洗过的曲名（重建管线会读；`目录名 -> 清名`）
train-work/gt/              人工 GT（.txt + .ly/.mid/.pdf/.jpg）
train-work/qa_sample/       抽检：随机 40 首的"原谱前 3 行带 + 转写 token"对照页（index.html）
train-work/dup_melodies.tsv 内容级重复清单（只列/只移，不删）
train-work/purity2_admit.txt 新纯度判据认为**该放行**的 667 个（试点验证后再决定是否吸收）
```

**关于来源溯源**：本想写进 score 的 `tag=` 字段，实测**下游 `parse_scores.py` 会把
`tag=` 重写成空**（展开文件里是 `tag=`），所以改用旁路文件 `source_map.tsv`。

---

## 三、token 格式

每个 token = `[时值前缀][低八度逗号][数字][高八度撇号][附点]`

| 部分 | 取值 | 含义 |
|---|---|---|
| 时值前缀 | 无 / `q` / `s` / `d` / `h` | 四分 / 八分 / 十六分 / 三十二分 / 六十四分 |
| 低八度 | `,` × 1-3 | 每个逗号降一个八度 |
| 数字 | `1`-`7` `0` `x` | 音级 / 休止 / 念白（打击记号）|
| 高八度 | `'` × 1 | 高一个八度 |
| 附点 | `.` | 附点 |

例：`q,5.` = 低八度 5 的八分附点音符；`'1` = 高八度 1 的四分音符。

---

## 四、只保留纯简谱（本轮加的过滤）

**目的**：吉他和弦谱/六线谱上的数字会被当音符读进来（实测污染 2.6 万+ 音符）。

**判据**（`tools/kind_detect2.py`）：

| 特征 | 定义 | 阈值 |
|---|---|---|
| `nline` | "单行最长连续暗段 ≥ 55% 页宽"的行数 | ≥ 5 判非纯 |

**灰度阈值自适应**（`max(60, 页均值-25)`）：固定 128 抓不到**淡扫描件**的谱线 ——
实测钢琴五线谱《我喜欢》（页均值 230）用 128 时 `nline=0` 混过过滤器，自适应后 `43` 判对。

原理：五线谱/六线谱的谱线横跨整页；简谱的圆滑线是**弧**，单行连续段短。

**实测标定**（自适应 nline）：

| 样本 | nline | |
|---|---|---|
| K歌之王 / 十年 / 去看拉萨河（密集纯简谱）| 0 / 0 / 0 | ✅ 纯 |
| **恋着多喜欢**（纯简谱，页面上有照片+猫+动漫）| **0** | ✅ 纯 |
| 我喜欢（淡钢琴五线谱）/ 消失的爱 | 43 / 11 | ❌ 非纯 |
| 一切还好 / 最佳损友 / 第一次（吉他）| 7 / 67 / 54 | ❌ 非纯 |
| 陈奕迅-十年 / Dear U（吉他弹唱谱，六线谱印得淡）| 202 / 167 | ❌ 非纯 |

**为什么不用"整行横向覆盖"(`wide`)做判据**：它会被**照片**骗 —— 歌谱页常配歌手照/
卡通图，暗区让 wide 飙高。实测纯简谱《恋着多喜欢》wide=62，若用 "nline≥5 或 wide≥45"
会把它连同 341 个谱、33,595 个音符一起**误杀**。`wide` 仍会算出来存进 tsv，只作参考。

**漏检（已知）**：极少数"六线谱被数字打断得很厉害"的吉他谱 nline 很低（如《淘汰》2、
《老公老公我爱你》0）会漏过 —— 但漏过只是多几个音符噪声，误杀则是整首丢失，取轻。

**另一类漏检**：五线谱的谱线被**小节线**打断时，单行最长段可能只有 30-40% 页宽
（实测《丁小琴编-19打起手鼓唱起歌（正谱）》34%、9 个长段行），与纯简谱重叠
（《K歌之王》45%、5 行）—— **标定后确认这个特征分不开这个案例**，故不调阈值（避免误杀）。
好在这些谱**转出 0 音符**，转换器本来就跳过（要求 ≥10 音符），不进交付物。

**收尾的三道安全网**（`finalize.py` 第 2/2b/2c 步）：
| 步骤 | 移出对象 | 实测 |
|---|---|---|
| `apply_kind_filter.py` | 纯度门判非纯 | 700+ |
| `quarantine_highx.py` | 念白 x 占比 ≥30%（全库均值 3.4%）| 21-23 / 1.2-1.5k 音符 |
| `quarantine_empty.py` | 0 音符结果（五线谱漏网/转写失败）| 206 |

这条门**已做进转写管线**（`jp_transcribe.transcribe()`），新爬的源根本不会转非纯谱。

---

## 五、同名歌取最优版本

同一首歌常有多个版本（简谱/钢琴谱/吉他谱/不同分辨率）。打分依次比较
（`tools/pick_best.py`）：

1. 纯简谱优先
2. `nline` 越小越纯
3. `wide` 越小越纯
4. 标题带"简谱"
5. 分辨率 `w×h`（越大越好 —— 小图放大后发虚，识别差）
6. 标题越短

**标题归并的两个坑**（都会丢歌）：
- 砍破折号后缀 → `田光歌曲选-380放学的铃声响了` 被砍成合集名，**92 首并成一首**
- 剥括号 → `童年（晨枫词_陈雄曲）` 和罗大佑《童年》并成一首

修法：破折号后缀不砍；括号里含**词/曲/演唱**等区分信息的保留，只剥编配类注释。

---

## 六、已知限制（诚实清单）

1. **非人工校对**：token 准确率两个口径 ——
   - spring《春天在哪里》锚点：**OK=86**（117 token / GT 114）
     ⚠️ 加"小图放大"(JP_MINW=700→950)后从 OK=87 变为 86（spring 图宽 749px，现在会被放大）。
     这个代价换来了**整段前奏不再丢**（《富士山下》引子、《卖报歌》引子），净收益更大。
   - 4 首 GT 图（`train-work/gt/*.jpg`）：**约 64%**
     （快乐父子俩 89.7% · 时间都去哪了 93.2% · 排排坐 100% · 兄弟抱一下 18.5%）
   - 口径：只比 数字/八度/声部

   > **踩坑记录**：`token_json` 原先只认"前置时值"(`q3`)，不认人手写 GT 常用的"后置"(`3q`)，
   > 导致 GT 的八分音符全被当成四分 → 匹配率虚低到 31.5%。jianpu-ly 官方说明
   > "字符顺序无所谓，`s1` 与 `1s` 等价"，已改成两种写法都认，数字回到 63.7%。
   > 跑评测前若发现匹配率异常低，先查这个。
2. **八度偶误**：低八度点有时漏读或多读（`,5` 应为 `5`）
3. **前奏/小字**：引子常用小一号字，靠"小图放大到 1200px"救；字号再小就丢
4. **混排谱**：吉他弹唱谱即使过滤后，仍可能有少量六线谱数字混入（已隔离大部分）
5. **流行歌不全**：源站有就收，没有就没有（周杰伦/陈奕迅部分歌曲源站本身缺失）

---

## 七、怎么重建

```bash
# 1) 转写某个源（增量，已存在的跳过）
py -3.13 tools/transcribe_source.py images-prep/jianpucn-pop

# 2) 收尾（纯度过滤 + 版本择优 + 重建 scores + 下游）
py -3.13 tools/finalize.py

# 3) 爬更多流行歌手
py -3.13 tools/crawl_pop.py 400 40

# 4) 评测（用 train-work/gt/ 里的图+同名txt）
py -3.13 tools/eval_gt_images.py
```

环境：`py -3.13`（3.14 没有 torch）、`JP_BATCH=8`、`JP_MAX_H=5000`、`JP_X_RECHECK=1`。

---

## 八、关键工具

| 脚本 | 作用 |
|---|---|
| `tools/jp_transcribe.py` | 主转写器（几何切块 + Qwen3-VL-2B 识别 + 纯简谱门）|
| `tools/crawl_pop.py` | jianpu.cn 歌手页爬虫（流行歌）|
| `tools/kind_detect2.py` | 纯简谱检测（nline + wide）|
| `tools/pick_best.py` | 同名版本择优 |
| `tools/finalize.py` | 收尾流水线（一键）|
| `tools/autopilot.py` | 无人值守：等转写 → 收尾 → 评测 → 再爬 → 转写 → 再收尾 |
| `tools/eval_gt_images.py` | GT 评测（正确口径：转 GT 自带的那张图）|
