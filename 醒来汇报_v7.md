# 醒来汇报 v7：原子化训练实验结果（如实）

时间: 2026-08-27 | 你在睡觉期间的工作

## 〇、你的思路：原子化训练 — 已完整实验

**做法**（`tools/synth_atoms.py`）：枚举全部单音符 token **1479 个**（7 数字 × 6 时值 × 3 升降 × 5 八度 × 附点 + 休止 + x），每个独立渲染成图（拍号适配时值、隐藏拍号显示），标注即 token。

## 一、实验轨迹（三次训练）

| 版本 | 数据 | 上海人 | 小星星 | 春天在哪里 | 原子识别 |
|---|---|---|---|---|---|
| **v6**（此前最优）| 整曲 600×3ep | **22/35, 低八度7/7** | **42/42** | - | - |
| v7（整页原子图×1+整曲）| 2079×2ep | 9/35 | 42/42 | 33/74 | 47/100 |
| v7b（裁剪特写原子图×3+整曲）| 5037×2ep | 0/35 | 2/42 ❌ | 56/74 | 56/100 |
| **v7b+回火**（原子→整曲3ep）| 600×3ep | 16/35, 低八度6/7 | 28/42 | 20/75 | 40/100 |

**关键发现（诚实）**：
1. **裁剪特写原子图教"看字卡"**：原子识别 47→56，但训练集 88% 是卡片 → 整曲排版能力崩溃（小星星 42→2）
2. **回火（原子记忆后用整曲重训）**恢复了部分排版能力（小星星 2→28、上海人 0→16），但冲淡了原子记忆（56→40）
3. **结论：v6 仍是综合最优**（上海人 22/35 + 小星星 42/42 + 低八度 7/7）。你的原子化方向是对的，但"孤立卡片"与"真实排版"差距太大，纯训练配比调不回来

## 二、v6 批量已完成（上次汇报后追加）

- 130 张全量转写：`train-work/batch_v6/`（130/130 过 score.py）
- `scores-v6-draft/`：128 份草稿（剔除 2 首吉他谱：歌声与微笑、数鸭子），128/128 过 score.py
- 注释头已按你的要求 `#`→`%`（jianpu-ly 规范）
- 与 7b 同曲 35 首对比：v6 音符密度是 7b 的 2-4 倍
- 你指出的歌声与微笑错误 `6, 6'` vs 正确 `6 6,`（八度方向）已记录

## 三、你醒后可做

1. **决定**：v6 保持为当前版本（我推荐），继续打 s/q 时值 + 低八度方向；或试"原子图内嵌真实排版"（新思路：把原子音符渲染进整曲小节，而非孤立卡片）
2. **看**：`train-work/real_eval_v6/`（8 张核心图）、`train-work/gt/`（你的 GT 存档：小星星/上海人/春天在哪里）
3. **标注**：3-5 首真实图 GT 混训仍是最高杠杆（真实渲染风格直接教给模型）
4. **入库**：`scores-v6-draft/` 128 首草稿（MBID 补录后可用）

## 四、文件

- 模型：`models/jianpu-lora-v6`（最优）、`jianpu-lora-v7`/`v7b`（实验）
- 数据：`train-data-atoms/`（1479 原子图+manifest）、`train-data-v7/`（原子+整曲混合）
- 工具：`tools/synth_atoms.py`、`merge_v7.py`、`eval_v7.py`、`postprocess_batch.py`、`validate_batch.py`、`make_drafts_from_batch.py`
- 报告：`训练报告.md`（v4→v7b 全史）、`train-work/eval_v5_v6.md`、`train-work/eval_v7b_final.log`
