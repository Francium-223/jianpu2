# 醒来汇报 v9：语料大修 + 流行歌源 + 纯简谱过滤（最终状态）

时间: 2026-09-16 夜 | 你梦游期间的自主工作
路线: **Qwen3-VL-2B 零样本管线**（不是 v6/v7 的 LoRA 微调路线）

---

## 〇、一句话

你交代的三件事全做完并验证：**只含纯简谱 ✓ · 同名取最优版本 ✓ · 调号行泄漏已修 ✓**；
另外**新开了流行歌数据源**（jianpu.cn），语料从早上的 1073 首有效谱 / 12.9 万音符
涨到 **1900+ 首 / 29 万音符**，乱码归零。

---

## 一、交付物

| 项 | 位置 | 说明 |
|---|---|---|
| 转写结果 | `batch-out/*.txt` (+`.png` 标注框图) | token 序列 |
| **曲谱（主交付物）** | `jianpu-db-out/scores/` | jianpu-db 格式，拍号识别 0 失败 |
| **HF 数据集** | `train-work/jpdbtest/out.jsonl` | 副本产出，**未动你的 jianpu-db 仓库** |
| 来源映射 | `train-work/source_map.tsv` | 标题→来源站→目录（溯源用）|
| 数据集说明 | `DATASET.md` | 格式/过滤判据/已知限制/重建方法 |
| 隔离区 | `batch-out-bad/` `-dup/` `-suspect/` `-empty/` | 被剔除的（**只移不删**）|

> 具体数字见文末"最终统计"，或直接跑 `py -3.13 tools/verify_deliverable.py`（7 项总验收）。

---

## 二、修了什么（每个都有实测证据）

### 组 A：整行/整段丢失（你在框图上肉眼发现的）
| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 1 | 《两只老虎》第二结尾整行无框 | 数字 h/w=1.20-1.29，全卡在"瘦高≥1.4"阈值下 | 阈值降到 1.15；谱头区仍用严格 1.4 |
| 2 | 《卖报歌》引子整行无框 | 引子的严格 frac(0.15) 比标题行(0.31)还低，被当谱头丢掉 | 谱头边界判据加"下划线≥6 条" |
| 3 | 修 2 后又发现整行只出 1 个块 | 高度门 `h_ref` 被 `:||:(` 记号(h=38/26)抬高，把 h=18 的真数字全杀 | `h_ref` 改用按行带的可变阈值 |
| 4 | 前奏整段变休止符 | 前奏用小一号字(13px)，卡在"数字高≥14"下 | 小图放大门槛 700 → **950** |

### 组 B：污染（把非简谱内容读成音符）
| # | 现象 | 修法 |
|---|---|---|
| 5 | 《富士山下》等把吉他六线谱数字读进来 | **纯简谱门**（见第三节）|
| 6 | 161 个"0 音符"空谱污染语料 | 被门挡掉的谱**不再写空 txt**；已存在的由收尾隔离 |
| 7 | 21 个"念白 x 占 30-79%"的错谱（全库均值 3.4%）| 收尾加安全网：高 x 率隔离 |

### 组 C：其他
| # | 现象 | 修法 |
|---|---|---|
| 8 | `1=F 3/4` 调号行被当音符（你要修的）| 装配阶段丢"前两个行带里数字<4"的 |
| 9 | 同名不同曲被并成一首（`童年（晨枫词_陈雄曲）` vs 罗大佑《童年》）| 括号含**词/曲/演唱**时保留 |
| 10 | `田光歌曲选-380放学的铃声响了` 被砍成合集名，92 首并成 1 首 | 不砍破折号后缀 |
| 11 | 只看几何会选中"图大但转不出东西"的版本 | 版本择优加**已转出音符数**判据 |
| 12 | 框图横跨两三行（行带和歌词粘连）| 框改用块内**主连通域**的紧致包围盒 |
| 13 | 下游 `parse_scores.py` 会吃掉 `tag=` | 来源改用旁路文件 `source_map.tsv` |
| 14 | 标题带曲集序号（`10信念`）| 剥序号前缀，但**保留纯数字歌名**（`1874`）|

---

## 三、纯简谱过滤器（这轮最花功夫的地方）

**目的**：吉他和弦谱/六线谱上的数字会被当音符读进来 —— 实测污染 2.6 万+ 音符。

### 最终判据
**"单行最长连续暗段 ≥ 55% 页宽"的行数**（`nline`），**≥5 判非纯**；
灰度阈值**自适应**（`max(60, 页均值-25)`）。

原理：五线谱/六线谱的谱线横跨整页；简谱的圆滑线是**弧**，单行连续段短。

### 踩的三个坑（都记在代码注释里）
1. **固定阈值 128 抓不到淡扫描件** —— 钢琴五线谱《我喜欢》（页均值 230）`nline=0` 混过
   → 改自适应阈值 → `nline=43` 判对 ✓
2. **"整行横向覆盖"(`wide`) 会被照片骗** —— 纯简谱《恋着多喜欢》（页上有歌手照+猫+动漫）
   `wide=62`，若用 "nline≥5 或 wide≥45" 会**误杀 341 个谱 / 33,595 音符**
   → **丢掉 wide**（nline 不受照片影响，该谱 nline=0 ✓）
3. **想再降阈值抓五线谱 → 分不开** —— 五线谱被小节线打断后最长段 34%/9行，
   而纯简谱 K歌之王 45%/5行、恋着多喜欢 40%/41行，**区间重叠**
   → 保持保守（误杀整首 ≫ 漏掉一个 0 音符的谱）

### 实测标定（自适应 nline）
| 样本 | nline | |
|---|---|---|
| K歌之王 / 十年 / 去看拉萨河（密集纯）| 0 / 0 / 0 | ✅ 纯 |
| 恋着多喜欢（纯·带照片）| 0 | ✅ 纯 |
| 我喜欢（淡钢琴谱）/ 消失的爱 | 43 / 11 | ❌ 非纯 |
| 陈奕迅-十年 / Dear U（吉他弹唱，六线谱淡）| 202 / 167 | ❌ 非纯 |
| 丁小琴编-19（五线谱，被小节线打断）| 0 | ⚠️ 漏（但转出 0 音符，不进交付物）|

**已做进管线**：`jp_transcribe.transcribe()` 开头的门 → 新爬的源**根本不会转**非纯谱。

---

## 四、新增数据源：jianpu.cn（流行歌）

### 摸清的入口（踩了 3 个坑）
- ❌ 按字数分类的列表页 = 按上传时间排 → 全是冷门原创
- ❌ 搜索接口两种编码都不对（站点自己坏了）
- ✅ **歌手页可用**（`/g/ch/chenzuoxun.htm` 列出该歌手全部作品）

### 爬到的歌手（约 40 位）
陈奕迅 · 张靓颖 · 光良 · 张敬轩 · 林忆莲 · 蔡幸娟 · **罗大佑** · **李宗盛** · **黄品源** ·
**莫文蔚** · **梁静茹** · **孟庭苇** · 谢雨欣 · 王宏伟 …

### 爬虫的两个关键 bug
1. **已存在的歌被直接跳过** → 页面上的"歌手/相关曲谱"链接永远采不到 → 队列枯竭（只拿 3 首就停）
2. **相关链接不按类型过滤** → 顺着钢琴伴奏谱滚到**莫扎特 185 部**（清掉 125 个漂移目录）

### 怎么保证只收纯简谱
列表项带类型标签（`[简谱]`/`[吉他谱]`），但**只在相关曲谱里**，`<title>` 里没有
→ 不靠标签，靠第三节的**几何纯度门** ✓

---

## 五、QA 体系（8 个脚本，纯 CPU）

| 脚本 | 查什么 | 抓到过什么 |
|---|---|---|
| `qa_corpus.py` | 空谱/格式异常/密度 | 161 个空谱污染 |
| `qa_highx.py` | 念白 x 占比异常 | 21 个错谱（30-79% x）|
| `qa_merge.py` | 版本归并质量 | `童年` 误并 |
| `qa_pick.py` | 择优结果抽查 | 《童年》只转出 53 音符 |
| `qa_scores.py` | scores 格式 | 全过（%END/title/拍号/正文）|
| `qa_titles.py` | 标题干净度 | 全过（0 后缀/0 ID/0 mojibake）|
| `qa_octave.py` | 八度/时值分布 vs GT | 无系统性偏差 |
| `check_false_neg.py` | 纯度门漏检 | 187 个漏检（26 个有污染）|
| `verify_deliverable.py` | **7 项总验收** | 一条命令给结论 |

**GT 评测的正确口径（踩坑）**：GT 对应的是**特定那张图**。按标题在 batch-out 里找会配到别的
编排版本（春天在哪里配到 254 音 vs GT 114 音 → 17.5%，毫无意义）。正确做法：转 GT 自带的那张图
（`eval_gt_images.py`，4 组"图+同名txt"）。

### ⚠️ 又一个测量口径坑：token 两种写法

第一次跑 GT 评测得到 **31.5%**，远低于 spring 锚点的 76% —— 查下去发现是**解析器**的锅：

| 写法 | 例子 | 用途 |
|---|---|---|
| 前置时值 | `q3` `'1q`→`q'1` | **本管线输出** |
| 后置时值 | `3q` `'1q` | 人手写 GT / jianpu-ly 谱面常用 |

jianpu-ly 官方说明："字符顺序无所谓，`s1` 与 `1s` 等价"。但 `token_json` 原先**只认前置**，
于是 GT 的 `3q` 被解析成 `digit='3q', beam=0` → 八分音符当成四分、`'1q` 连八度也丢了
→ **GT 与我方 token 永远匹配不上** → 匹配率虚低。

修好后（两种写法都认，纯 CPU 重新评分，不用重转）：

| 曲目 | 修前 | **修后** |
|---|---|---|
| 兄弟抱一下 | 3.3% | **18.5%** |
| 快乐父子俩 | 32.5% | **89.7%** |
| 排排坐 | 96.9% | 96.9% |
| 时间都去哪了 | 50.5% | **91.3%** |
| **合计** | 31.5% | **63.7%** |

**真实准确率 63.7%**（与 spring 锚点 76% 同量级）。以后再看到匹配率异常低，先查这里。

---

## 六、已知限制（诚实清单）

1. **非人工校对**：以 spring《春天在哪里》GT 为锚点，约 **76%**（112 token / GT 114 / OK=87）
2. **八度偶误**：低八度点有时漏读或多读（`,5` 应为 `5`）
3. **前奏/小字**：靠"小图放大到 1200px"救；字号再小就丢
4. **五线谱漏网**：被小节线打断的五线谱 `nline` 可能为 0（但转出 0 音符，被收尾隔离）
5. **吉他谱漏网**：六线谱被打断得厉害时 `nline` 很低（如《淘汰》2）→ 多几个音符噪声
6. **流行歌不全**：源站有就收，没有就没有（周杰伦大部分歌源站本身缺失）

---

## 七、无人值守流程

`tools/autopilot.py`（日志 `train-work/autopilot.log`）—— 转写一结束就自动：

1. **收尾流水线** `finalize.py`（7 步）
   纯度扫描 → 移出非纯 → 移出高念白率 → 移出 0 音符 → 版本择优 → 重建 scores → 来源映射 → 下游
2. **GT 评测**（4 组图+GT）
3. **再爬一批歌手**（400 首）+ 转写
4. **再收尾** + 最终统计 + **交付物总验收**

### 万一自动驾驶被中断（会话结束会杀掉后台进程）

**一条命令补完**：

```bash
py -3.13 tools/finish.py
```

它会依次跑：收尾流水线 → GT 评测 → 最终统计 → **交付物总验收**，日志 `train-work/finish.log`。
跑完看最后那行"总验收: 全部通过 ✓"即可。

---

## 八、你醒后可以选（我按自己的判断做了 1）

1. **继续扩歌手** —— 爬虫通了，过滤器保证只收纯简谱 ← 已在自动跑
2. 标注 3-5 首真实图 GT —— 这是提升准确率**最高杠杆**的一步（真实排版直接教给模型）
3. 修"八度误检"（需要先量化：`qa_octave.py` 已就绪，但要有更多 GT 才能定位）
4. 换更高分辨率的版本（同一首歌常有多个版本，择优已在做）
