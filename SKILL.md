# 简谱旋律检索 Skill（jianpu-melody-search）

**一句话**：给一段哼唱/简谱数字串（例如 `51223323323531`），从 6000+ 首歌的简谱库里找出这是哪首歌，并给出处（哪个谱站、哪一份谱、命中在谱里的第几个音）。

---

## 1. 实测指标（可直接引用）

基准集：**华流金曲 100 首**（邓丽君/周杰伦/凤凰传奇/张学友/陈奕迅/Beyond/朴树/李宗盛…，见 `train-work/bench_final100.txt`）。
索引：全库已转写曲谱 **7757 首歌 / 10514 份谱**（`batch-out` + `batch-out-dup`，按曲名分组，**已做内容级去重**）。
**基准集 100/100 首都在索引里。** 查询：从该首歌自己的谱里随机截 L 个音（每首 2 段 → 200 次查询）。

| 哼多少音 | 唱错几个 | 错音方式 | **Top-1** | Top-3 | Top-5 |
|---|---|---|---|---|---|
| 11 | 1 | 只错到**相邻音级**（最像真人） | **95.00%** [91.0, 97.3] | **99.5%** | **99.5%** |
| 11 | 1 | 随机换成任意其它音（更狠） | 94.50% [90.4, 96.9] | 99.5% | 100% |
| 11 | 0 | — | 100.00% [98.1, 100] | 100% | 100% |
| 9 | 1 | 相邻音级 / 随机 | 71.50% / 73.50% | 87.5% / 91.5% | 95.0% / 94.0% |
| 7 | 1 | 相邻音级 / 随机 | 25.5% / 29.5% | 41.5% / 51.0% | 52.0% / 57.5% |

方括号是 **Wilson 95% 置信区间**；完整表见 `train-work/DELIVERY.md` 与 `train-work/retrieval_eval_*.tsv`。

**漏唱/多唱一个音也能查**（`--indel 1`，见 `tools/melody_retrieval_indel.py`，100 次查询）：

| 真值长度 | 哼唱偏差 | **Top-1** | Top-3 | Top-5 |
|---|---|---|---|---|
| 13 音 | 漏/多 1 个音 | **81.00%** [72.2, 87.5] | 87.0% | 88.0% |

**留一版本（cross-version，更接近「凭记忆哼唱」）**：查询片段取自版本 A，**把 A 从索引里排除**，
必须靠这首歌的**其它版本**认出来。100 首里有 81 首存在第二个版本（162 次查询）：

| 每段长度 | 段数 | **Top-1** | Top-3 | Top-5 | 两版这段一致时 | 两版有差异时 |
|---|---|---|---|---|---|---|
| 11 音 | 1 段 | **50.6%** [43.0, 58.2] | 58.6% | 60.5% | **100%** | 19.2% |
| 11 音 | 5 段 | **66.0%** [58.5, 72.9] | 73.5% | 74.1% | 94.8% | 50.0% |
| 15 音 | 5 段 | **69.1%** [61.6, 75.7] | 76.5% | 77.2% | 96.0% | 57.1% |

**推荐宣传口径（两个数一起说才稳）**：

> **同一个曲谱库内**（哼的就是库里那份谱）：哼 11 个音、1 个音唱错 → 歌级 **Top-1 95.00%**（95% CI 91.0–97.3）、Top-3/Top-5 99.5%。
> **换成同一首歌的另一份谱去查**（更像凭记忆哼）：哼 1 段 11 音 → Top-1 50.6%；**哼 5 段 11 音 → 66.0%、Top-5 74.1%；哼 5 段 15 音 → 69.1%、Top-5 77.2%**。
> 拆开看：只要那段旋律在库里**确实存在**，Top-1 就是 **95–100%**；掉分全部来自「同一首歌的另一份谱，这段记谱本来就不同」（中位数差 1–4 个音）。

**"是不是匹配器不行？"——已用数据排除**（`tools/melody_retrieval_contour.py`）：
再加一路**与调无关的"间隔轮廓"**匹配（相邻音音级差 mod 7，真人记不全唱名但记得走向时就是这个），
两路取优只把 Top-1 从 51.2% 抬到 52.5%；诊断显示 **79 次失败里只有 2 次（2.5%）是"只是移调/换记法"**，
其余 77 次是**旋律真的不同**。所以跨版本差距的来源是**谱与谱之间的差异**，不是检索算法。

**四条必须一起说的口径**（不然数字会被挑）：

1. **歌级检索**：同一首歌的不同版本（吉他版/弹唱版/扫描版…）命中任意一份都算对。
2. **并列交代**：查询片段取自目标歌自己的谱，所以目标歌的距离必为 0，而别的歌常含同一串音。
   排名按 `1 + (严格更近的歌数)`，**并列时目标歌排最前**；同时给出「并列按名次算」的下界
   （11 音 1 错时该下界 44.5%，随机错音时 52.0%）。11 音 0 错时并列组平均 **1.6 首**，
   7 音 0 错时高达 **40 首** —— 所以 **7 音不作为宣传口径**。
3. **错音模型两种都报**：随机错音有时反而**更好**（跳到不常见的音，片段更独特）；
   相邻音级错音更接近真人，也更容易撞上别的歌。给两种是为了不挑对自己有利的那个。
4. **这是检索环节的指标**：输入是唱名串（简谱数字），"哼唱录音 → 唱名串"那一段不在这个数字里。

---

## 2. 怎么用

```bash
# 人看（可以一次给多段：同一首歌哼几句，分数相加再排序 —— 实测跨版本 Top-1 51.3% → 70.3%）
py -3.13 tools/melody_query.py "51223323323531" --top 5 --show
py -3.13 tools/melody_query.py "5122332332" "335313517" --top 10
py -3.13 tools/melody_query.py "5122332332531" --top 5 --indel 1   # 允许漏/多唱一个音(慢约 10 倍)

# 给 AI agent 用（JSON）
py -3.13 tools/melody_query.py "51223323323531" --top 3 --json
```

```json
{
  "query": "51223323323531",
  "query_len": 14,
  "songs_indexed": 6649,
  "best_mismatch": 0,
  "tie_at_best": 1,
  "results": [
    {
      "rank": 1, "title": "暖暖", "mismatch": 0, "exact": true,
      "source": "jianpucn-54884", "score_file": "暖暖__jianpucn-54884",
      "match_at_note": 29, "score_window": "135122332332353117"
    }
  ]
}
```

输入规则：只认 `1`–`7`（简谱唱名），空格/下划线/连字符随便加，其它字符忽略。
输出里 `source` 与曲库 `scores/*.txt` 里的 `source=` 字段一致，可直接溯源核对。

---

## 3. 数据与出处

* **谱源**：jianpu.cn、qupu123（中国曲谱网）、jianpujia —— 每份谱都在 `jianpu-db-out/scores/*.txt`
  里写了 `source=<站>-<id>`，逐份可回溯到原网页。**7054 份成品曲谱，100% 带出处。**
* **图片 → 唱名**：自研管线（行带切分 + 音符块分类 + Qwen3-VL 数字识别），
  **只收纯简谱**：五线谱/六线谱/简线对照/混排会被纯度门挡掉并移出语料（隔离不删，见 `batch-out-bad/`）。
  名字像"钢琴/双手/吉他"的谱共 1334 份，其中 1015 份已被纯度门隔离。
* **转写质量**：与手写 GT 同口径复评：**4 首 / 551 个音，音高袋口径 97.6%，序列口径 66.6%**
  （`train-work/gt_report.txt`）。样本小，引用时必须带样本量。
* **来源总表**：`train-work/source_map.html`（16219 条：曲名 / 站点 / 站点 ID / 图片目录 / 转写结果 / 检索链接 / 备注）。
* **交付说明**：`train-work/DELIVERY.md`。

---

## 4. 已知限制（先自己说，别等对面问）

1. **匹配引擎只做等长比对（Hamming）+ 单删变体**：
   `--indel 1` 已能容忍**漏唱/多唱一个音**（把查询的所有单删变体和它自己一起比对），
   但**不支持**一次漏两个音、或漏音+错位（换拍/改词序）这类情况。漏两个音的代价还没测。
2. **音频环节不在指标里**：目前入口是唱名串。要做成端到端 Skill 还需"哼唱 → 唱名"的转写，
   这一段没有测（也**不能**把这 95% 说成"听歌识曲准确率"）。
3. **曲目覆盖偏华语流行/民歌**：西方曲目少（曲谱站上西方多为器乐谱，过不了纯度门）。
   实测可得率：华语流行/港乐/怀旧国语 ≈ 100%（抽样），儿歌 10%，西方 12%。
4. **同歌多版本必然并列**：返回里给 `tie_at_best`，不隐藏；靠"名字更短/非改编版/qupu123 优先"破并列，
   这只是排序启发式，不是音乐判断。漏/多唱 1 个音时并列会更严重（短片段更不独特）。
5. **简谱本身的多声部/转调写法**：目前按单声部主旋律处理，和声/复调谱会被纯度门隔离。
6. **查询片段取自转写结果**：单段查询时，查询和目标来自**同一份谱**，这会让指标偏乐观。
   已用**留一版本**实验量化：79/100 首有第二个版本时，单段 Top-1 只有 51.3%（见上表）。
   真实场景请用**多段**（`melody_query.py "片段1" "片段2" …`）并引用留一版本的数字。
   附：实测 44%（L=11）的片段在两个版本里**完全一样**，其余中位数差 1 个音 —— 也就是说
   这个差距主要是**乐谱之间的差异**，不是检索器的缺陷。
7. **连音线保留、圆滑线丢弃**：写 scores 时保留 `~`（连音线，下游 `score.py` 白名单里认它），
   丢弃 `(` `)`（圆滑线，下游白名单不含、且我们在 token 流里对它俩的语义没有验证过）。
   丢弃圆滑线不改变音高与时值。

---

## 5. 复现

```bash
py -3.13 tools/melody_retrieval_eval.py --sweep                    # 随机错音全表(200 次查询)
py -3.13 tools/melody_retrieval_eval.py --sweep --errmode neighbor # 相邻音级错音全表
py -3.13 tools/melody_retrieval_indel.py --len 13 --sub 1 --err 0 --n 1   # 漏/多唱 1 个音
py -3.13 tools/gt_transcribe_eval.py                               # 手写 GT 复评
py -3.13 tools/verify_deliverable.py                               # 交付物校验(7 项)
```

结果表：`train-work/retrieval_eval_rand.tsv`、`retrieval_eval_neighbor.tsv`、`retrieval_indel.tsv`
（列：L / 错音数 / 错音模型 / 查询数 / Top1 / Top3 / Top5 / 并列下界 / 并列组均值）。


---

## 本次自动同步（tools/sync_docs.py，2026-09-23 11:27）

- 成品谱 **7346** / `source=` **7346** / JSONL **7346 首 1,435,061 音符** / 索引 **7757 首歌 10514 份谱**
- 覆盖率（`train-work/coverage_report.md`）：
  | ① 已交付 `jianpu-db-out/scores` | 265/319 | **83.1%** |
  | ② + `batch-out`(待入库) | 269/319 | **84.3%** |
  | ③ + `batch-out-dup`(乐观上界) | 275/319 | **86.2%** |
- 最近一次评测末行：
  - 同版自匹配(neighbor): `11	2	neighbor	200	123	163	175	30	23.77`  (1790125301)
  - 留一版本(holdout): `15	0	neighbor	5	162	110	118	120	105	81	3	97.8	56.0`  (1790125720)
  - 漏音(indel): `13	1	0	neighbor	100	80	85	87	59	46.19`  (1790125968)
  - 轮廓(contour): `两路取优	11	1	162	83	97	101`  (1790126047)
- 已知问题：见 `train-work/qa_head_artifact.md`（页眉标题被读成音符）与 `train-work/frag_ambiguity.log`（短片段判别力）。
