# 醒来汇报 v10：交付收口 + 全量曲名清洗 + 旋律检索指标（可对外）

时间: 2026-09-22 凌晨 | 你睡觉期间的自主工作
路线: 本地 **Qwen3-1.7B（纯文本）洗曲名** + Qwen3-VL 图片转写 + **歌级旋律片段检索**

---

## 〇、一句话

你要的"能量化的指标"做出来了，而且口径能站得住：

> **哼 11 个音的旋律、其中 1 个音唱错，在华流金曲 100 首里歌级 Top-1 命中 95.00%（95% CI 91.0–97.3），Top-3/Top-5 99.5%。**
> 哼 13 个音、中间漏掉或多了 1 个字 → Top-1 81.00%（95% CI 72.2–87.5）。
>
> **但更值钱的是这条**（后半夜补的"留一版本"实验）：上面那个 95% 是**拿谱对谱**。
> 换成"**凭记忆哼、库里是另一份谱**"（查询取 A 版、索引里排除 A）：单段 **50.6%**，
> **哼 5 段 11 音 → 66.0%；哼 5 段 15 音 → 69.1%（Top-5 77.2%）**。拆开看：只要那段旋律在库里
> **确实存在**，Top-1 就是 **95–100%** —— 掉分全部来自"同一首歌的两份谱记法本来就不同"，不是检索器的毛病
> （这一点还用"与调无关的间隔轮廓"匹配**验证过**：79 次失败里只有 2 次是移调，见第三节 #22）。

同时交付物收口完毕（**校验 8 项全过**）、**15702 条曲名全量洗净**（8083 条有改动，
不达标的从 191 清到 **38**），另外挖出并修掉了 **848 个"只发 PNG 所以从没被转过"的谱** +
**482 份"曲名不同但旋律完全相同"的重复谱**（只移不删，可逆），
还按证据**吸收了 667 个被旧纯度判据误杀的简谱**（试点+后验都验过，只移不删）。最终：

**成品曲谱 7346 份 / 语料 14738 份 / `source=` 覆盖 100.0% / JSONL 7346 首 143.5 万音符 /
检索索引 7757 首歌 / 基准集 100/100 全覆盖。**

---

## 一、指标（这一节可以直接对外说）

**基准集**：华流金曲 100 首（邓丽君/周杰伦/凤凰传奇/张学友/陈奕迅/Beyond/朴树/李宗盛…，`train-work/bench_final100.txt`）。
**索引**：全库已转写曲谱（`batch-out` + `batch-out-dup`，**7176 首歌 / 9683 份谱**）按**曲名分组**（同歌多版本算一首）。
**基准集 100/100 首都在索引里**。**查询**：从该首歌自己的谱里随机截 L 个音；每首 2 段 = 200 次查询。

| 哼多少音 | 偏差 | 错音模型 | **Top-1** | Top-3 | Top-5 |
|---|---|---|---|---|---|
| 11 | 错 1 个音 | 相邻音级（最像真人） | **95.00%** [91.0, 97.3] | 99.5% | 99.5% |
| 11 | 错 1 个音 | 随机换任意音（更狠） | **94.50%** [90.4, 96.9] | 99.5% | 100% |
| 11 | 全对 | — | 100% [98.1, 100] | 100% | 100% |
| 9 | 错 1 个音 | 相邻音级 / 随机 | 71.5% / 73.5% | 87.5% / 91.5% | 95.0% / 94.0% |
| 7 | 错 1 个音 | 相邻音级 / 随机 | 25.5% / 29.5% | 41.5% / 51.0% | 52.0% / 57.5% |
| 13 | **漏/多 1 个音** | — | **81.00%** [72.2, 87.5] | 87.0% | 88.0% |

**必须一起说的四句**（不然数字会被挑）：

> 全部数字都是**最终语料（7436 首歌 / 9958 份谱，基准集 100/100 全覆盖）**上测的，每次自匹配 200 次查询、
> 留一版本 162 次、漏多唱 100 次。原始表：`train-work/retrieval_eval_rand.tsv`、`retrieval_eval_neighbor.tsv`、
> `retrieval_holdout.tsv`、`retrieval_indel.tsv`。

1. **歌级检索**：同一首歌的吉他版/弹唱版/扫描版命中任意一份都算对（用户查的是"哪首歌"，不是"哪一页"）。
2. **并列交代**：查询片段取自目标歌自己的谱，距离必为 0，而别的歌常含同一串音。排名按
   `1 + 严格更近的歌数`（并列时目标歌排最前），**同时给出"并列按名次算"的下界**。
   11 音 0 错时并列组平均 **1.6 首**（语料变大后更干净）；7 音 0 错时高达 33 首 → **7 音不作为口径**。
3. **两种错音模型都报**：随机错音有时反而更容易（跳到不常见的音、片段更独特），
   相邻音级错音更接近真人。都报，是为了不挑对自己有利的那个。
4. **这是"检索环节"的指标**：输入是唱名串（简谱数字）。「哼唱录音 → 唱名串」不在其中，
   **不能**说成"听歌识曲准确率"。这条写在 `SKILL.md` 的"已知限制"里。

---

## 二、转写质量（另一条线，别和上面混）

**手写 GT 复评**（`train-work/gt_report.txt`，谱图就在 `train-work/gt/`，用当前管线重转比对）。
样本这轮从 4 首扩到 8 首（把只有 PDF 的那几首用手写 GT 自己的排版件渲染出来转）：

| 组 | 首数 | GT 音符 | 音高袋口径 | 序列口径 |
|---|---|---|---|---|
| **扫描件**（和语料同分布） | 4 | 551 | **97.6%** | 66.6% |
| 排版 PDF（jianpu-ly/LilyPond 输出） | 4 | 1488 | 84.1% | 9.2% |

**引用转写准确率时用"扫描件"那组**：排版件是衬线数字 + 下划线当减时线，和语料的站点扫描件
不是一个分布（小星星那份只有 38.5%，就是这个原因）。这本身是个有用的发现：**管线对排版件
没调过**。样本量小，引用时必须带样本量。

---

## 三、修了什么（每条都有实测证据）

### A. 曲名清洗（你要的"全量喂给文本模型"）

| # | 问题 | 根因/证据 | 修法 |
|---|---|---|---|
| 1 | 旧写法要跑到早上 6 点 | 单序列逐字生成 1.02 s/条 × 16219 | 真并行批 + 够一行就停 → **0.155 s/条**（全量 40 分钟） |
| 2 | "一次 16 条塞一条序列"整批废 | 模型**照抄输入**（19/20 原样吐出） | 改成**一条序列一个曲名**，照抄降到 4/20 |
| 3 | 书名号残留 | 你要"书名号也不要有" | prompt + `clean_one()` 确定性剥离（221 条），并做后处理回补已跑批次 |
| 4 | 清名只改了文件名、`title=` 还是旧名 | `to_score` 内部 `mb_title or title_of(name)` | 显式把清名传下去，**文件名与 title= 同源** |
| 5 | 撞名文件 `%` 行和真实文件名不一致 | 撞名加 `_2` 但 `%` 行按 title 拼 | `%` 行改用最终文件名（下游按 % 行认名） |
| 6 | 机械规则永远修不干净 | `草原丁喜萨克斯原创曲…` 这类 | 清名**接进重建管线**（`to_jianpu_db` 读 `title_clean.tsv`），每次重建自动保持干净 |

结果：**15702 条全量喂入，8083 条有改动，复核不过 1 条**；仍有 182 份名字不达标 → 写 `todo=refine the filename` 字段（不藏在注释里）。

### B. 检索评测器（两个会让数字失真的坑）

| # | 坑 | 后果 | 修法 |
|---|---|---|---|
| 7 | **文件级**排名 | 目标谱被**同一首歌的另一个版本**抢掉名次 → 该算命中却算失败 | 改**歌级**：按曲名分组，取组内最好成绩 |
| 8 | 并列按曲名排序 | 谁名字靠前谁赢 = 随机扣分（旧日志那个 82% 正好等于现在的"下界"） | `rank = 1 + 严格更近的歌数`，并列时目标排最前，**同时给下界** |

### C. 管线与交付物

| # | 问题 | 根因/证据 | 修法 |
|---|---|---|---|
| 9 | **848 个谱从没被转过** | `pick_page()` 只找 `*.jpg`，qupu123 有的曲子只发 `.png`（《领悟》就是） | jpg/jpeg/png 一起收；补转**已完成**：语料 6644→7094、成品谱 6601→7054、基准集 100/100 |
| 10 | 连音线被丢掉 → 时值错 | 写 scores 时只放行音符 token，67% 的谱里有 `~`/`(`/`)` | `~` 保留（下游 `score.py` 白名单认它），`(` `)` 丢弃（白名单不含，语义未验证） |
| 11 | 4 份谱没有 `source=` | `grab_sheet.py` 抓谱时**把原页 URL 扔了** | 抓取时写 `_source.txt`；无站点 ID 的名字加来源兜底（`unknown` 时也写清本地路径） |
| 12 | 交付文件名里藏零宽空格 | 源站标题含 U+200B（63 个目录名） | 全链路剥离；《算什么男人》因此才能匹配上基准集 |
| 13 | 《1874》被改名成 `未命名-…` | 兜底判据不认数字 → 纯数字歌名被判"没内容" | 判据加 `0-9` |
| 14 | 每次重建都要重认 6000+ 份拍号（~50 分钟 GPU） | `finalize` **删掉**旧 scores → 拍号无法复用、人工标记也丢 | 改成移到 `scores-prev/` 复用 → 重建从 40 分钟降到几分钟，`todo=` 全部保住（本次 182 份一个没丢） |
| 15 | GT 评测算出 1.6% 这种假数字 | 按文件名找"同一首歌"配错版本（《小星星》配到 319 音的另一个版本） | 新写 `gt_transcribe_eval.py`：**转 GT 自己的谱图**，配对无歧义 |
| 16 | **JSONL 只剩 1 首**（下游崩了没人发现） | 我把 URL 塞进 `source=` 值（`source=jianpucn-150657  http://…`），下游 `score.py` 把这个值当**目录名**用 → `WinError 123` → `parse_scores.py` 第 1 首就崩；而 verify 只查"可解析/有曲目"，拿昨天旧文件混过去了 | `source=` 只留**单个安全 token**（站点-id），URL 移到 `%` 注释行；verify 加**条数校验**（JSONL 曲目数须 ≥ 0.9×scores）。修完 JSONL **1 首 → 7054 首 / 135.5 万音符** |
| 17 | 45 个以《开头的谱被**合并成同一首歌** | 评测脚本用 `[（( 《]` 切分组键，切完是空的 → 45 个不相关的谱塌成一组，检索评测会串味 | 空键退回原名；`melody_query` / 三个评测器 / `bench_pick` / `bench_status` 六个脚本一起修 |
| 18 | 191 个名字仍不达标（站点壳子/署名残留/丢字节） | 机械规则不敢乱砍（把《不懂钢琴》砍成《不懂》） | 第二轮**只喂这 191 条**给模型 + **逐条人工核对**（我改了 40 条模型猜错的）+ **按站点 id 传播到孪生目录**（同一页面爬两次的目录）→ **191 → 36 条**，剩下的是真没名字或规则对《口琴五级：…》这类真曲名的误报 |
| 19 | **我自己引入的数据损坏**（要认） | `fix_names_round3.py` 用了**模糊包含匹配**，把原始名只有一两字的正常歌（《爱》《家》《你》共 25 条）改成了 `总有爱`/`藏在浓雾里的朴家人` | 从重建前的 `scores-prev` 按 `source=` 对回真实 `title=`，逐条还原（10 条）+ 找不到旧值的退回原始名（11 条）+ 精确修正 4 条；脚本注释里写明"改名字必须**精确匹配目录名**" |
| 20 | 英文名的 `_` 被误判成"丢字" | 本管线的文件名约定就是 `_` ← 空格（`Because_of_You`），判可疑是误报 | 规则①只对**含中文**的名字生效 |
| 21 | **515 份谱是"整串旋律完全相同"的重复** | `pick_best.py` 按**曲名**归并，而同一网站常把同一首歌用**不同曲名**挂两遍（`稻香` / `稻香（稻香）`、mojibake 孪生），于是绕过归并 | 新写 `dedupe_by_content.py`：按音高串整串相同分组（≥40 音），按"能读的名字 > 有清名 > 名字短 > qupu123"留一份，**只移不删**（清单可逆）→ batch-out 里重复组 **254 组/261 份 → 0** |
| 22 | 跨版本掉分"是不是匹配器不行" | 有人会这么质疑 51.3% 那个数 | 加一路**与调无关的"间隔轮廓"**匹配（相邻音音级差 mod 7）→ 两路取优只把 Top-1 从 51.2% 抬到 **52.5%**；诊断：79 次失败里**只有 2 次（2.5%）是"只是移调/换记法"**，其余 77 次旋律真的不同。**结论：差距来自谱与谱的差异，不是算法** |
| 23 | 扩库的"净新增率"没量过 | 按歌手页抓的谱很可能都是已有歌的另一个版本 | 新写 `expand_yield.py` 量全量：**301 份转写里只有 3 个新曲名（1.1%）**，266 个是已有曲名 —— 说明"堆版本"≠"扩曲目"，**扩库的天花板不在抓取量** |
| 24 | **纯度门可能误杀了 667 个纯简谱** | 旧判据数"长横线行数"，而**黑色标题底框/logo** 能单独把纯简谱顶过阈值；新判据只数**成簇的细线**（五线谱的本质） | 实测：隔离区 5118 份里 **667 个（13%）**按新判据该放行；现有语料在新判据下**误杀 0 份**（无回归） |
| 25 | 这 667 个到底能不能收？（**已按证据执行并后验**） | 不能只看判据，得看真转出来的质量 | ①试点抽 80 页真转（**不写语料**）：x 率 2.69% vs 基线 2.98%，0 率 4.95% vs 7.25% → 执行吸收；②吸收后再**按站点分别比**（整体平均会被站点构成混淆）：**吸收批比原有语料低 0.5–0.8 个百分点**（qupu123 78.6% vs 79.4%、jianpucn 76.9% vs 77.7%、jianpujia 79.7% vs 80.2%），坏尾（数字占比<50%）在 jianpucn 上更薄（0.0% vs 1.9%）、jianpujia 上更厚（2.5% vs 1.2%）→ 结论：**质量基本持平、略逊一点，没有实质下降**（先前说的"反超"是站点构成造成的假象）。全程只移不删，清单 `purity2_admit.txt` + `lowdigits_moved.tsv` |
| 26 | 吸收 667 个到底扩了多少新曲目？ | 预期是"+10% 语料"，得实测 | 真相：667 个里 **145 个被按曲名归并成重复版本、38 个转不出音符**，真正新增条目 **47 个（+0.7%）** —— 又一次印证"瓶颈不在抓取量" |

---

## 四、跑完了（六条链全部收工，不用你管）

| 任务 | 干什么 | 结果 |
|---|---|---|
| `jp_refine_titles` | 全量曲名清洗 | 15702 条，8083 条有改动 |
| `jp_tidy_titles` | 去书名号 + 剪残留标点 | 221 条，复核 0 不过 |
| `jp_bench_run` | 补基准集缺谱 → 重建 → 评测 | 91/100 → 98/100 |
| `jp_deliver_run` | 标 `todo=` / 来源表 / HTML / 校验 / 交付说明 | 校验 7 项全过 |
| `jp_night_extend` | 抓 59 首名人名曲（158 个新目录）→ 转写 → 自检 → 重建 → 评测 → 刷新交付物 | 语料 +200 余份 |
| `jp_gt_eval` | 手写 GT 复评 | 音高袋 97.6% |
| `jp_demo` | 检索演示图 | `train-work/demo_match.png` |
| `jp_png_expand` | 补转 848 个 PNG-only 谱名（展开 1582 个目录）→ 重建 → 双轮评测 → 刷新交付物 | **语料 6644→7094、成品谱 6601→7054、覆盖 100/100**，09:00 收工 |

> 交付快照已按**最后一次重建**刷新（`scores` 7054 份 / `source=` 覆盖 100% / `verify` 8 项全过）。

### 四之三、又起了一条扩库链（还在跑）

现有图片**基本转完了**（16380 个谱目录里 14478 个已有结果，backlog 只剩 89 个可转），
所以扩库要靠**抓新谱**：第一轮按歌手页爬的 31 位已爬完，这轮补**44 位没爬过的华语歌手**
（周杰伦/王菲/陈奕迅/那英/刀郎/腾格尔/凤凰传奇/李宗盛/罗大佑/降央卓玛/蒋大为…），
每位最多抓 20 首 → 转写 → 收尾 → 重跑两轮评测 → 抽检 → 刷新交付物。
任务名 `jp_artist_expand`，预计 2–3 小时（GPU）。

> ⚠️ 先说结论：**按歌手页扩库的净新增曲目只有 1.1%**（301 份转写里 3 个新曲名，266 个是已有曲名，
> 净增成品谱 +167 份，都是"同一首歌的另一个版本/另一个记法"）。
> 真正还能扩的是另一个口子：**纯度门误杀的 667 个纯简谱**（见第三节 #24），正在试点验证。
> 结论：语料规模的天花板不在"抓多少"，而在"**换源**"和"**判据放不放行**"。

后面还排了两条：`jp_final_dedupe`（再去重 + 收尾 + 全量重测 + 刷新交付物）→
`jp_purity_pilot`（纯度门放宽试点，抽 80 页真转比质量，**不写语料**）。

---

## 四之二、追加：把"偏乐观"量化掉（留一版本实验）

主评测的查询片段取自目标谱**自己**，等于"拿谱对谱"。真实用户哼的是**脑子里的旋律**。
所以又做了一版：**查询取版本 A、把 A 从索引里排除**，逼它靠同一首歌的**另一份谱**认出来
（100 首里 79 首有第二个版本，158 次查询）：

| 每段长度 | 段数 | Top-1 | Top-3 | Top-5 | 两版这段一致时 | 两版有差异时 |
|---|---|---|---|---|---|---|
| 11 音 | 1 段 | **50.6%** [43.0, 58.2] | 58.6% | 60.5% | **100%** | 19.2% |
| 11 音 | 5 段 | 66.0% [58.5, 72.9] | 73.5% | 74.1% | 94.8% | 50.0% |
| 15 音 | 5 段 | **69.1%** [61.6, 75.7] | 76.5% | 77.2% | 96.0% | 57.1% |

**结论（这条比 95% 更值钱，因为它解释了 95% 是怎么来的）**：

* 只要那段旋律在库里**确实存在**（两份谱这段一样），Top-1 就是 **98–100%** —— 检索器没问题；
* 掉分全部来自「**同一首歌的另一份谱，这段记谱本来就不一样**」：实测两份谱的片段差异中位数
  1–4 个音（44% 的片段两版完全相同，其余有差）；
* 真人不会只哼一句 —— **给 5 段后 Top-1 从 51.3% 回到 70.3%**，所以查询工具改成支持
  一次给多段（`melody_query.py "片段1" "片段2" …`，分数相加再排序）。

这条也顺手修了一个评测 bug：45 个以《开头的谱因为"分组键为空"被**合并成同一首歌**，已修。

---

## 五、交付物

| 项 | 位置 | 说明 |
|---|---|---|
| **曲谱（主交付物）** | `jianpu-db-out/scores/` | **7054 份**，全部带 `source=<站>-<id>`（覆盖 100%），校验 8 项全过 |
| 检索能力（Skill 入口） | `tools/melody_query.py` | 哼一段（或一次多段）→ 查歌，支持 `--indel 1`、`--json` |
| **指标与口径说明** | `SKILL.md` | 数字、置信区间、四条口径、已知限制、复现命令 |
| 交付说明（一页速览） | `train-work/DELIVERY.md` | 自动生成，含曲库/指标/清洗/GT/自检 |
| 来源总表 | `train-work/source_map.html` | 16223 条：曲名/站点/站点ID/图片目录/转写结果/检索链接/备注 |
| 检索演示图 | `train-work/demo_match.png` | 用片段 `51223323323531` 查到《暖暖》后，把谱里命中的那行裁出来 |
| 手写 GT 复评 | `train-work/gt_report.txt` | 音高袋 97.6%（4 首 551 音） |
| 自我抽检 | `train-work/qa_report.txt` | 空谱/极少/异常多/格式异常 token |
| **抽检样本（能逐条核对）** | `train-work/qa_sample/index.html` | 随机 40 首：**左边原谱前 3 行带、右边转写 token**，一眼比得出对不对 |
| 隔离区（只移不删） | `batch-out-bad/ -dup/ -empty/ -suspect/` | 被纯度门与去重剔除的 |

---

## 六、你要做的

只有一件（我不动你的仓库）：

```bash
cd D:\Documents_D\jianpu-db
py -3.13 parse_scores.py      # 把 jianpu-db-out/scores 复制过来后解析
git add -A && git commit -m "jianpu2: 6600+ scores (带 source= 出处)"
```

---

## 七、顺手解的那个谜

你之前问的 `512233 233 23531` —— 库里**唯一完全一致**的是 **《暖暖》**：

```
py -3.13 tools/melody_query.py "51223323323531" --top 5 --show
 1. 暖暖   ✓完全一致  来源=jianpucn-54884   谱里这段: 135122332332353117
```
