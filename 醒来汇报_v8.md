# 醒来汇报 v8：转写管线大修 + 流行歌曲源 + 纯简谱过滤

时间: 2026-09-16 | 你梦游期间的工作
路线: **Qwen3-VL-2B 零样本管线**（不是 v6/v7 的 LoRA 微调路线）

---

## 一、你给的三个任务，全做完了

| 任务 | 状态 |
|---|---|
| 修「调号行泄漏」（`1=F 3/4` 被当音符） | ✅ 已修并验证 |
| 版本择优（同名多版本选最优） | ✅ 已实现并应用 |
| 只转纯简谱 + 把前面的逐个查一遍 | ✅ 过滤器已做进管线 + 全库查完 |

---

## 二、这轮修的 9 个 bug（附证据）

| # | 现象 | 根因 | 修法 |
|---|---|---|---|
| 1 | 《友谊天长地久》附点全丢 | 附点吸收用固定 12px，大图上附点离数字 16-25px | 改成按数字高度比例 |
| 2 | 标题行被当音符转进去 | 几何判据分不开"标题文字行"和"音符+歌词粘连行带" | 可疑行带交模型判"音符/文字" |
| 3 | 小节线被当数字 | 小节线满足数字判据（高14-45、高>宽、宽≥3） | 加 `高 < 3.5×宽` |
| 4 | 照片被当成谱（4 首儿歌全空） | pick_page 取"最大文件"，照片比线稿大 | 优先 qupu123 的 002.jpg，否则取第一个竖版页 |
| 5 | 44 张谱静默丢失 | mojibake 目录名含 C1 控制字符，写文件失败 | `safe_name()` 解码 + 清洗 |
| 6 | 190 张 A4 扫描被跳过 | `JP_MAX_H=3000` 太严 | 提到 5000 |
| 7 | 124 张谱转出 0 音 | 高分辨率扫描数字 ~90px，超过 `高≤50` 判据 | 过宽页缩到 2000px |
| 8 | **整行丢失**（你在框图上发现的） | 数字 h/w=1.20-1.29，全部卡在"瘦高≥1.4"阈值下 | 阈值降到 1.15 + 谱头区用严格阈值 |
| 9 | **前奏整段变休止符** | 前奏用小一号字（13px），卡在"数字高≥14"下 | 小图放大门槛 700 → **950** |

**bug 8、9 是你在图上看出来的**——我自己测不出来。

---

## 三、新增数据源：jianpu.cn（流行歌）

### 摸清的入口（踩了 3 个坑）
- ❌ 按字数分类的列表页 = 按上传时间排 → 全是冷门原创
- ❌ 搜索接口两种编码都不对（站点自己坏了）
- ✅ **歌手页可用**（`/g/ch/chenzuoxun.htm` 列出该歌手全部作品）

### 爬到的歌手（约 40 位）
陈奕迅 · 张靓颖 · 光良 · 张敬轩 · 林忆莲 · 蔡幸娟 · **罗大佑** · **李宗盛** · **黄品源** ·
**莫文蔚** · **梁静茹** · **孟庭苇** · 谢雨欣 · 王宏伟 …

### 爬虫的两个关键 bug（都记在代码注释里）
1. **已存在的歌被直接跳过** → 页面上的"歌手/相关曲谱"链接永远采不到 → 队列枯竭（只拿 3 首就停）
2. **相关链接不按类型过滤** → 顺着钢琴伴奏谱滚到**莫扎特 185 部**（清掉 125 个漂移目录）

---

## 四、纯简谱过滤器（v2 版，已验证）

### 判据
**"单行最长连续暗段 ≥ 55% 页宽"的行数**（`nline`）

- 五线谱/六线谱的谱线横跨整页 → 一页积累几十条
- 简谱的圆滑线/延音线是**弧** → 单行连续段短

实测（9/9 分类正确，中间是空的）：

| 样本 | nline |
|---|---|
| K歌之王 / 十年 / **去看拉萨河（密集纯简谱）** | **0** |
| 第一次 / 富士山下C调编配（吉他） | 24 / 23 |
| 夜空中最亮的星（五线谱）/ 童话总谱 | 41 / 166 |

### 中途踩的两个坑
1. 先用"整行覆盖 >60% 页宽"→ **误杀密集纯简谱**（《去看拉萨河》的圆滑线横跨多音）
2. 六线谱线**印得很淡** → 用暗像素判据漏检（《最佳损友》nline=1 却是吉他谱）

### 已做进管线
`jp_transcribe.transcribe()` 开头的"纯简谱门"，`JP_KINDFILTER=1` 默认开 → **以后爬新源根本不会转非纯谱**

### 全库结果
5455 个目录逐个查完：**纯简谱 4590 / 非纯 855**；非纯里有结果的 700 个已移出到 `batch-out-bad/`（隔离不删）

---

## 五、版本择优

同一首歌常有多个版本（简谱/钢琴谱/吉他谱/不同分辨率）。打分依次比较：

1. 纯简谱优先（nline < 5）
2. nline 越小越纯
3. 标题带"简谱"
4. 分辨率 `w×h` 越大越好 ← 《十年》原图只有 511px，放大后发虚、识别差
5. 标题越短越好

4650 目录 → **4161 首**，落选 **467** 个移到 `batch-out-dup/`

**《富士山下》的教训**：库里只有差版本(720px带水印) + 2 个吉他混合谱，**好版本从没被爬进来**——你给的 URL 是第一个。我已把它补进语料。

---

## 六、当前数字

| 项 | 数值 |
|---|---|
| 转写文件 | 2037 |
| 有效（有音符） | 1984 |
| 音符总数 | **290,995** |
| 乱码 `?` | **0** |
| `jianpu-db-out/scores/` | **1939**（拍号失败 0）|
| `train-work/jpdbtest/out.jsonl` | **2102 首 / 316,770 音符** |
| 隔离区 | `batch-out-bad/` 700 · `batch-out-dup/` 364 |

**对照今天早上**：有效谱 1073 → **1984**；音符 129,892 → **290,995**；`?` 2000 → **0**

---

## 七、还在跑（你醒来时应该已完成）

**已交给"自动驾驶"**（`tools/autopilot.py`，日志 `train-work/autopilot.log`）——它会自动依次做：

1. ⏳ 等当前 **1454 个谱**增量重转结束（694 新爬 + 760 个 [700,950) 中图）
2. 收尾流水线：纯度扫描 → 移出非纯 → 版本择优 → 重建 scores → 下游
3. **GT 评测**（用 `train-work/gt/` 里 4 组"图+同名txt"：快乐父子俩 / 兄弟抱一下 / 排排坐 / 时间都去哪了）
4. **再爬一批歌手**（400 首）
5. 转写新爬的
6. 再收尾一次 + 最终统计

**所以不需要人盯**，你回来直接看 `train-work/autopilot.log` 和下面的数字。

## 七之二、QA 又抓到一个 bug

`tools/qa_corpus.py` 扫描发现语料里有 **161 个"0 音符"的空谱** ✗。查证：它们是
**吉他弹唱谱**（如《Fly Away》——和弦图 + 六线谱），已被"纯简谱门"正确挡掉，
**但空结果仍被写成了空 txt 文件**污染语料。

修：`transcribe_source.py` 里被门挡掉的谱**不再写 txt**；已有的空谱由收尾流程移出到 `batch-out-bad/`。

## 七之三、GT 评测的正确口径（踩坑记录）

我先写了个按标题在 batch-out 里找对应转写的评测 → **错了**：GT 对应的是**特定那张图**，
按标题会配到别的编排版本（春天在哪里配到 254 音的版本 vs GT 114 音 → 匹配率 17.5%，毫无意义）。

正确做法：转写 GT 自带的那张图再比（`tools/eval_gt_images.py`）。

## 七之四、过滤器的**漏检**也被抓出来了（第二轮加固）

`tools/check_false_neg.py` 用另一个特征交叉复核了 4590 个"纯简谱"，发现 **187 个漏检** ✗
（被判纯、其实是吉他/五线谱），其中 **26 个已转出音符，合计 7,495 个污染** ✗
（如《一切还好》625 音符、《淘汰》、《老公老公我爱你》）。

**单用 nline 不够** —— 六线谱被数字打断时"最长连续段"变短，nline 就很低甚至为 0。
加上第二个特征 `wide`（灰度<128 且整行横向覆盖>60% 页宽的行数）取 **OR**：

| 样本 | nline | wide | 判定 |
|---|---|---|---|
| K歌之王 / 十年 | 0 | 0 / 11 | 纯 ✓ |
| **去看拉萨河**（密集纯简谱）| 0 | **22** | 纯 ✓（没误杀）|
| 第一次 / 富士山下C调 | 37 / 23 | 43 / 28 | 非纯 ✓ |
| 最佳损友 / 夜空五线谱 | 46 / 41 | 51 / 47 | 非纯 ✓ |
| **一切还好 / 淘汰 / 老公老公我爱你** | **0** | **98 / 124 / 169** | 非纯 ✓（新抓到）|

**阈值 wide≥45**：让《去看拉萨河》(22) 安全通过，吉他谱(79~169) 全挡。**10/10 样本正确**。

全库重跑：纯简谱 4590→**4740**，非纯 855→**1102**（多抓 247 个），其中会多移出 **163 个结果 / 2,914 音符**。
管线里的门（`jp_transcribe._nline_big`）和版本择优打分都已同步用上 `wide`。

## 八、你醒后可做的选择（我按自己的判断选了 1）

1. **继续扩歌手**（爬虫已通，过滤器保证只收纯简谱）← 推荐
2. 修"八度误检"（`,5` `,6` 该是 `5` `6`，低八度点偶有误读）
3. 处理"前奏/引子"更多残留（现在靠放大救，字号再小就没了）
4. 把那 3 首被隔离的吉他版《富士山下》… 不用管，已经对了

## 九、文件

**核心工具**（本轮新增/改动）
- `tools/jp_transcribe.py` — 主转写器（改了：行带阈值 1.15、谱头边界、`h_ref`、调号行过滤、纯简谱门、小图放大 950）
- `tools/crawl_pop.py` — jianpu.cn 歌手页爬虫（流行歌）
- `tools/kind_detect2.py` — 纯简谱检测器（nline）
- `tools/pick_best.py` — 版本择优
- `tools/finalize.py` — 收尾流水线（一键：过滤→择优→重建→下游）
- `tools/transcribe_source.py` — 按源/按名单转写

**名单文件**（`train-work/`）
- `kind2.tsv` — 全库纯度明细
- `pick_best.tsv` / `drop_dup.txt` — 版本择优结果
- `bad_sheets.txt` / `headfix_sheets.txt` / `small_need.txt` / `mid_need.txt` / `next_batch.txt` — 各轮增量名单

**数据源**（`images-prep/`）
- `jianpucn-pop/` — **1999 个**流行歌目录（新）
- `qupu123-crawl/`(3412) · `jianpujia-crawl/`(493) · `hot-crawl/`(10) — 原有
