# 简谱图像转写管线：从几何切分到可训练数据集

**一份完整的技术报告（commit paper）**

---

## 摘要

本文报告一套从**简谱（numbered musical notation）图像**到**结构化可训练数据**的完整转写管线的设计、实现与实验。

管线采用**几何切分 + 视觉语言模型识别**的组合架构：用连通域分析与投影法把谱面切成"音符块"，用几何规则判定时值下划线、八度点、附点等非数字符号，再让一个 **2B 参数的视觉语言模型（Qwen3-VL-2B）** 逐个识别音块里的数字，最后组装为简谱线性 token 序列。

在自建的手工标注集上，管线在《春天在哪里》上取得 **OK=87**（旧基线为 83），在《排排坐》上 81% 命中；而在"音符行与歌词行粘连"的复杂谱面上表现较差（36% / 12%），该局限已定位到具体机制。

在此基础上，本文将其应用于 **4007 张简谱图像**的批量转写，产出 **367 套转写结果（296 有效，54,507 个音符）**，并进一步转换为 **jianpu-db 数据集的曲谱格式**（281 首，含自动拍号识别），以及 **HuggingFace 合规的 JSONL**（仅含已整理曲目）。

本文还记录了对上游数据集 jianpu-db 的若干架构级改动：外部标识体系的重构、曲目状态标记、以及 **jianpu-ly 的 `KeepLength`（黏性时值）语义展开**——该展开在 jianpu-ly 中只有内部实现而无公开接口，本文按其次序语义重新实现并修正了作用域问题。

最后，本文如实记录了**七处失败或被回退的优化尝试**，供后续工作避免重复。

---

## 1. 引言

### 1.1 背景

**简谱**（numbered musical notation，又称数字谱）是一种以阿拉伯数字 1–7 表示音级、以附加符号表示时值与八度的记谱法。它在中国大陆的群众歌曲、儿歌、民歌、流行曲谱中占据绝对主流，其图像资源在互联网上极为丰富。

然而，**大语言模型面对简谱几乎束手无策**。原因有三：

1. **简谱不是线性文本**。同一段旋律的语义分散在二维空间中：数字只给出音级，时值由数字**下方**的横线数量表示，八度由数字**上方或下方**的点表示，附点写在数字**右侧**，而**延音**则由独立的短横线表示。同一首曲子的"文本表示"与其"图像表示"之间的映射是二维的。
2. **简谱缺乏统一的机器可读格式**。虽有 `jianpu-ly` 这样的记谱语言，但其输入仍需人工编写，网络上的简谱绝大多数只是图片。
3. **标注数据稀缺**。公开的、可供训练的"简谱图像—旋律"配对数据几乎不存在。

### 1.2 目标

本项目（jianpu2）的目标是**把简谱图像自动转写为机器可读的 token 序列**，并以此**扩充公开数据集 jianpu-db**。

具体而言：

- **输入**：一张简谱图片（可能含标题、谱号、调号、拍号、歌名、多行音符与歌词）。
- **输出**：一条线性 token 序列，形如 `q3 q3 ,5 5. - 0 s'6`，其中：
  - 数字 1–7 表示音级，`0` 表示休止，`x` 表示念白/打击记号；
  - 前缀字母表示时值：`c` 四分、`q` 八分、`s` 十六分、`d` 三十二分、`h` 二分；
  - 前缀逗号表示低八度（`,5`），后缀撇表示高八度（`5'`）；
  - 后缀点表示附点（`5.`）；
  - `-` 表示延音。
- **副产品**：一张与转写**同一次运行**生成的可视化标注图（便于人工核验），以及可直接并入 jianpu-db 的曲谱文件。

---

## 2. 系统架构

### 2.1 总体流程

```
简谱图像
   │
   ├─(1) 二值化 + 行切分 ─────────────► 音符行 / 歌词行
   │
   ├─(2) 行内连通域分析 ─────────────► 数字块 / 延音杠 / 下划线 / 弧线 / 附点 / 八度点
   │
   ├─(3) 块聚类 ────────────────────► "音符块"（数字 + 其附属符号）
   │
   ├─(4) 块分类 ────────────────────► digit / dash / rest / empty
   │
   ├─(5) 数字识别（Qwen3-VL-2B）────► 1–7 / 0 / x
   │
   ├─(6) 几何符号判定 ───────────────► 时值(下划线数) / 八度(点数) / 附点
   │
   └─(7) token 组装 ────────────────► "q3" ",5" "5." "-" "0"
```

### 2.2 行切分

对二值图做**水平投影**，按投影为零的行间隔切分出行带。为容忍扫描噪声，采用 `ROW_GAP=8` 的间隙阈值（小于 8 像素的空隙不视为行分隔）。随后以"该行是否含足够多的小节线"（细高连通域）过滤掉标题行、歌词行等非音符行。

### 2.3 块切分与聚类

在音符行内做**连通域分析**，得到若干连通域。每个连通域带有 `(x0, y0, x1, y1, w, h)` 属性。随后按以下规则聚类为"音符块"：

- **数字**：高 ≥ 14、高 > 宽、宽 ≥ 6 的连通域；
- **下行附属**（时值下划线、低八度点）：x 落在数字 x 范围内或紧邻；
- **上行附属**（高八度点）：数据 x 范围内、数字上方；
- **右侧附属**（附点）：数字右缘右侧、且与数字同高；
- **独立延音杠**：见 §3.1。

### 2.4 符号识别

采用 **Qwen3-VL-2B-Instruct**（约 4 GB，bfloat16）作为数字识别器。相比端到端方案（见 §5），将识别任务限定为"看一张小图，输出一个字符"，使 2B 模型的能力足够且推理成本可控。

提示词：
```
这个简谱记号是什么? 只输出一个字符: 数字 1-7, 或 0(休止符),
或 x(念白/无音高/打击记号)。不要解释。
```

解码后以 `re.search(r"[0-9x]", s)` 提取首个字符，取不到则记为 `?`。

### 2.5 时值与八度判定

- **时值**：统计数字块内"位于数字下方"的细横线数量。由于扫描图中一条下划线常断成多个碎片，采用**先收集细碎片、再按 y 行聚类**的策略：同一 y 行（容差 2 像素）的碎片总宽 ≥ 4 像素才算作一条时值线。下划线数 0/1/2/3/4 对应 `c/q/s/d/h`。
- **八度**：数字上方的小点为高八度（`'`），下方且紧贴数字底部的小点为低八度（`,`）。低八度点须满足"距数字底 < 0.6×数字高 + 3 像素"，以排除更下方疑似歌词笔画的噪声。
- **附点**：位于数字右缘右侧、且 y 在 `[数字顶-2, 数字底+2]` 内的小点。

### 2.6 token 组装

```python
token = BEAM_PRE[beam] + ","*low + "'"*voice + digit + "."*dotted
```

其中 `BEAM_PRE = {0:"", 1:"q", 2:"s", 3:"d", 4:"h"}`。

---

### 2.7 一个片段的完整走查

为说明各环节如何衔接，这里用《THERE IS A REASON》中一小节 `6 1 2 2 1 5` 做逐步走查（该片段下方有一条共同下划线，共享十六分时值）。

**步骤 0：输入**
一行含六个数字的音符行，每个数字下方有下划线（其中若干条在视觉上连成一条长线），数字之间有小节线。

**步骤 1：二值化**
灰度图按阈值 `TOL` 转为布尔矩阵。此时数字笔画、下划线、小节线、以及下方歌词的一两笔都是"前景"。

**步骤 2：行切分**
水平投影找出该行的上下边界。判据是该行内含足够多的"细高连通域"（小节线）。若该行与下方歌词贴得太近（§8.3），此步会把它们合并——这正是复杂谱面失败之处。

**步骤 3：连通域分析**
在该行内做连通域标记，得到若干分量，每个带 `(x0,y0,x1,y1,w,h)`。此时可以看到：六个数字各是一个分量；下划线因扫描质量可能断成 3–5 段；小节线是一个细高的分量。

**步骤 4：块聚类**
以"高 ≥ 14 且高 > 宽"的分量识别为数字。随后为每个数字收集附属：

- 位于数字下方、x 落在数字范围内的细横线碎片 → 归属该数字的时值线；
- 位于数字上方的小点 → 高八度；
- 位于数字右缘右侧且同高的小点 → 附点。

**关键**：一条"共同下划线"会**跨过多个数字**。它被切成多段后，每段按 x 落到各自数字的范围内；落在**数字之间空隙**中的那几段，则因"中心点不在任何数字 x 内"而被判定为**候选独立延音杠**。

**步骤 5：延音杠过滤**
这正是 §3.1 修复的核心：若不做过滤，那些"落在数字之间的下划线碎片"会各自生成一个独立块，输出 `6 - 1 - 2 - 2 -`，即"满屏假延音"。

修复后，判据要求独立延音杠**中心点不落在任何数字的 x 范围内**。但在这个片段里，碎片恰好位于空隙中——**它仍会通过判据**。真正排除它们的是**时值线的 y 行约束**：这些碎片的 y 与"贴合数字的下划线"处于**同一水平线**，因此在"细横线是否属于某数字时值线"的判定中已被归入时值线，不再参与延音杠候选。

（这也是为什么 §3.1 的修复需要两步：先按中心点排除"数字正下方的线"，再由 y 行聚类把"同一行的空隙碎片"归并到同一条时值线。）

**步骤 6：块分类**
每个块被分类为：

- `digit`：含数字分量；
- `dash`：孤立延音杠；
- `rest`：矮小的椭圆形分量（休止符 `0`）；
- `empty`：无法归类的噪声块（丢弃）。

**步骤 7：数字识别**
所有 `digit` 块被批量送入 Qwen3-VL-2B（一次 8 块），得到 `6 1 2 2 1 5`。

**步骤 8：几何判定**
对每个块调用 `geo_detect`：

- `beam`：该数字下方的时值线数量 → 该片段为 `1`（八分）或经 y 行聚类后为 `2`（十六分）；
- `low` / `voice`：八度点数；
- `dotted`：附点。

**步骤 9：组装**
```
BEAM_PRE[1] + "" + "" + "6" + ""  →  "q6"
                                     "q1" "q2" "q2" "q1" "q5"
```
（若判定为十六分则为 `s6 s1 s2 s2 s1 s5`。）

**步骤 10：可视化**
同一次运行中，每个块的位置被画上矩形框，框内标注该 token。**图与文本来自同一份 meta 数据**，因此二者严格一致——这是"可核验性"的实现方式。

---

## 3. 关键问题与修复

本节记录转写精度提升过程中定位到的**八个具体缺陷**。所有修复都经过**同一张谱（spring）的回归验证**，确保不引入退化。

### 3.1 延音杠与"时值下划线"的混淆

**现象**：转写结果中每个音符后几乎都跟着一个 `-`，形成 `q3 - q3 - ,5 -` 的密集假延音。在《THERE IS A REASON》上，假 `-` 占 token 总数的比例高得离谱。

**根因**：延音判定原用一条宽松规则——"宽 > 12、高 ≤ 4、宽高比 > 2 的细横线即延音杠"。但**数字正下方的时值下划线恰好也满足该条件**，于是一条时值线被同时当作"数字的时值"和"独立的延音杠"，产生假 `-`。

**测量**：对真实谱面做统计后得到清晰区分：

| 类型 | 宽度 | 高度 | 与数字 x 重叠 |
|---|---|---|---|
| **时值下划线** | 21–54 | 2 | **是**（贴合数字）|
| **独立延音杠** | ≈11 | 2–4 | **否**（位于音符之间）|

**修复**：延音杠必须满足"**中心点不落在任何数字的 x 范围内**"：

```python
dc = (da[0] + da[2]) / 2.0
if any(s0 <= dc <= s1 for s0, s1 in digit_spans):
    continue      # 中心落在数字内 -> 是时值线, 归数字
```

**注意**：判定必须用**中心点**而非"任意重叠"——延音杠紧贴数字时 x 会与数字相接，用区间重叠判定会误杀真延音杠（这是本修复的第二次迭代）。

**效果**：
- 《THERE IS A REASON》：假 `-` 由"满屏"降至 **9 个 / 272 音（3%）**
- **spring：音数由 142 降至 113（GT 为 114，几乎完全对齐），OK 保持 87**

### 3.2 共同下划线与碎片化

**现象**：十六分音符的两条下划线、以及"多个音符共享的一条长下划线"被切分得支离破碎，导致时值判定错误（`s` 被读成 `q`）。

**根因**（依次定位到三个）：
1. 裁剪时把音符块右侧裁到数字底部，**切掉了下划线**；
2. 附点收集时用**动态扩大的 x1** 判断"右邻附属"，导致裁剪区域跨越到下一个音符；
3. 扫描图中一条下划线**断成多个碎片**，而判定要求"单个连通域宽 ≥ 10"，于是碎片全被否决。

**修复**：
1. 音符块的下边界放宽至 `bbot + 6`；
2. 附点判断改为相对**数字原始右缘**（`dx1_orig`）而非动态 x1；
3. 时值线判定改为**收集细碎片后按 y 行聚类**，以"该行碎片总宽"是否达标判定。

**效果**：spring 由 85 → 86 → **87**。

### 3.3 连音弧线被误认为音符

**现象**：音符块数量异常偏高（151 → 应为 142），多出的块出现在音符之间。

**根因**：连音线/圆滑线（一条弧）的连通域宽 ≈ 20、高 ≈ 8，恰好通过了当时"高 ≤ 8"的横线判定，被当成延音杠并生成了独立音符块（块内 y 起点甚至为负）。

**修复**：把延音杠判定的高度上限从 8 收窄到 **4**（纯横线），弧线因高度更大而被排除。

**效果**：音符块数 151 → 142，与人工计数一致。

### 3.4 附点在低八度点上的误判

**现象**：`d,5` 被转成 `d,5.`（凭空多出附点）。

**根因**：低八度点位于数字**右下方**，而附点判定只要求"在数字右侧"，于是低八度点被同时算作附点。

**修复**：附点增加 y 约束——必须在 `[数字顶-2, 数字底+2]` 的**同高带**内。

### 3.5 双高八度（`''`）泛滥

**现象**：转写结果中出现大量 `3''.`、`5''`、`1''` 等双高八度记号，占比达可疑水平（某次统计中 188/399 个文件含双撇，共 **1998 个** `''` token）。

**根因**：高八度点的计数没有上限，凡是数字上方出现两个小点（可能是扫描噪声、装饰点、或与上行的粘连）都会累加。

**修复**：将 `voice` 上限设为 **1**。依据是使用者明确指出"简谱里两个及以上高八度基本可忽略"。

**验证**：对两个最严重的文件做精准重跑：

| 文件 | 修复前 `''` | 修复后 | 音数 |
|---|---|---|---|
| 红星歌 | 99 | **0** | 518（不变）|
| 2026年欢乐笑开颜 | 83 | **0** | 534（不变）|

随后对全量 367 个结果重新统计，**双高八度残留为 0**。

### 3.6 `x`（念白/打击记号）未被识别

**现象**：原谱中的 `X` 符号（表示念白、无音高或打击）在转写结果中完全缺失。

**根因**：识别提示词只要求输出数字 1–7 或 0，模型遇到 `X` 时输出非数字，被正则过滤后判为 `?` 并跳过。

**修复**：提示词显式加入 `x`，正则改为 `[0-9x]`。

**效果**：《Together We Fight》由 0 个 `x` token 变为 **27 个**；全量统计出现 **1985 个 `x` token**。

### 3.7 休止符丢失时值

**现象**：`MBID` 转换后的曲谱中，休止符从 `q0` 退化为裸 `0`，与 GT 不匹配（该项曾导致 spring 由 87 掉到 81）。

**根因**：为修掉"`''0`（高八度休止符）"这一荒谬输出，曾把 `d == "0"` 一律特判成裸 `"0"`——**连时值前缀也一起丢掉了**。但简谱的休止符**有时值**（八分休止 `q0`、十六分休止 `s0`）。

**修复**：休止符保留时值（`BEAM_PRE[beam] + "0"`），仅去掉八度与附点。

**效果**：spring 由 81 回到 **87**。

### 3.8 谱页选择错误（源站特例）

**现象**：某源站（qupu123）的曲目全部转出"0 音"。

**根因**：该站的 `001.jpg` 是 **750×55 的标题条缩略图**，真正的谱页是 `002.jpg` / `004.jpg` / `006.jpg`（约 1400×1900）。

**修复**：不再死用 `001.jpg`，改为"**取该目录下文件最大的 jpg**"作为谱页。

---

## 4. 鲁棒性与工程问题

### 4.1 RGBA 图像无法保存为 JPEG

**现象**：部分曲目转写失败，报 `OSError: cannot write mode RGBA as JPEG`。

**根因**：源图为 RGBA/带透明通道；标注图与分页切片在保存为 `.jpg` 前未转换色彩模式。

**修复**：两处保存（渲染输出、分页切片）均加 `.convert("RGB")`。

### 4.2 超长"长条谱"拖垮批量

**现象**：某些 qupu123 曲目的谱图高达 7922 像素（多页拼接），单张切出 **2169 个音符块**，耗时 **24 分钟**。

**分析**：查看缩略图确认该图确实包含 5–6 页内容，2169 个音符是**真实的**（非误检）。因此耗时来自"音符数 × 单音符前向"的硬成本，**无法通过分页降低总时长**。

**处理**：
1. **分页**：按**行边界**把超长图切成若干页（每页 ≤ 2200 像素高），逐页转写后合并——好处是**进度可见、可断点续传**，避免一次卡 20 分钟；
2. **过滤**：把"高 > 3000 像素"的图直接跳过并记入 `skipped.txt`（本次跳过 29 张）。

依据是：这类长条图是"多首曲子拼在一张"，整体转写出的 2000+ 音符序列对数据集没有价值。

---

## 5. 失败的尝试（如实记录）

为避免后续工作重复踩坑，本节记录**七项被验证无效或有害**的方案。

### 5.1 端到端生成式坐标回归

直接让模型输出每个音符的边框坐标。**结果：召回率 0%**，输出退化为 `[10,10,10,10]` 之类的常量。**已放弃。**

### 5.2 端到端 token 生成

让模型看图直接输出整条 token 序列。**结果：输出垃圾**（如 `#'''`），完全不可用。**已放弃。**

### 5.3 用 Qwen 特征训练六个预测头

提取视觉特征后接 6 个分类头（时值/八度/附点等）。**结果**：spring OK 仅 **20**（Qwen2.5 基线为 83），且推理时输出恒定的 `q1.`、`q5`。根因是**推理时的特征与训练时的特征不一致**（预处理路径不同）。**已放弃。**

### 5.4 网格热力图 CNN 切分

用 CNN 预测音符位置的热力图。**结果**：合成数据上 99.7%，**真实谱面上接近 0**。说明合成数据与真实扫描件的域差异过大。**已放弃。**

### 5.5 批量推理以外的三种"加速"

| 尝试 | 预期 | 实测 |
|---|---|---|
| **crop 指纹缓存**（相同图案复用）| 省重复前向 | **88s vs 69s，更慢**——块图案实际不重复，指纹计算倒贴开销 |
| **降采样 crop**（0.7×）| patch 变少 → 更快 | **144s，更慢**——processor 又把图放大回 `min_pixels` 预算，白搭 resize |
| **降低 processor 像素预算** | patch 变少 | **150s，更慢**——Qwen3-VL 的 processor 不接受该属性 |

**唯一有效的加速**是**批量推理**（见 §6.1）。

### 5.6 若干被回退的切分参数

以下改动**均导致 spring 由 87 退化到 81/79**，已全部回退，仅供记录：

- 要求数字严格单字符（`re.fullmatch`）；
- 附属符号距离限制（数字底 + 20 像素）；
- 音符块下边界缩至 `bbot + 3`；
- 行间隙 `ROW_GAP` 由 8 改为 4；
- 提高小节线细高门槛（20/26 → 30/35）；
- 用"最大连通域"而非"任一连通域"判定延音杠。

---

## 6. 批量转写与数据集构建

### 6.0 为什么选择"逐块识别"而不是"整谱理解"

在进入工程细节之前，有必要说明本管线最基本的设计取舍。

一个自然的想法是：既然现在的视觉语言模型能读懂复杂的图表，为什么不直接把整张简谱图片丢给模型，让它输出整条 token 序列？

我们实际尝试过（§5.2），结果是完全不可用的垃圾输出。事后分析，原因有三层：

**第一层是输出长度**。一首曲子有 150–300 个音符，每个音符的 token 平均 2–3 个字符，因此单次生成需要 400–900 个 token 且**必须严格有序、一个不能错位**。生成式模型在长序列上的"漂移"是累积的：前 50 个音符即使全对，第 51 个音符的错误也可能让后续全部失去对齐。

**第二层是空间定位**。简谱的语义分布在二维空间：数字只是"音级"，时值在下方、八度在上/下方、附点在右侧。要求模型在一次前向中同时完成"定位所有符号 + 判定它们的两两从属关系 + 按音乐时序排序"，是把三个不同性质的任务耦合在一起。

**第三层是可验证性**。生成式的输出是"一个字符串"。当它错了，你无法知道是"哪个音符错了"还是"整体排序错了"。而分块方案中，每个音符块都有明确的**像素坐标**，可以直接画框可视化——这让"转写—检查—修正"形成闭环。

因此本管线选择：**把空间问题交给几何算法，把语义问题（这个数字是几）交给模型**。二者通过"音符块"这一中间表示衔接。这个划分使得：

- 几何部分的错误是**可定位的**（哪一行、哪个 x 区间），可以逐条规则修正；
- 模型部分的错误是**可枚举的**（数字 1–7、0、x 共 9 类），2B 参数足够；
- 整个系统的行为是**可解释的**（每个 token 都能追溯到"哪个块 + 哪条规则"）。

这也是为什么 §3 中八个修复全部是**几何判定**或**输出格式**问题，而没有一处需要重新训练模型。

### 6.1 批量推理

单张谱平均含 ~180 个音符块，若逐块前向则每张耗时 ~250 秒。改为**批量前向**（一次 8 块）后降至 ~187 秒，**约 25% 提速**，且输出序列与逐块**完全一致**。

**关键实现细节**：生成任务必须使用 **左 padding**：

```python
_proc.tokenizer.padding_side = "left"
```

若使用默认的右 padding，各样本的生成位置会与其 padding 对齐，导致解码错位。这是本方案能同时保证"批量"与"正确"的关键。

**为什么提速有限**：瓶颈在**视觉编码**（ViT 前向）。批量只是让多块并行，总计算量不变。加大多批量收益递减，而多进程无益（单张 GPU，只会互相抢占）。

### 6.2 热度优先：找"最可能被搜索的曲子"

数据集的长期价值取决于其**可被检索性**，因此应优先转写**知名度高**的曲目。本文用 **B 站播放量**作为热度代理指标。

**为什么不用 MusicBrainz**：MusicBrainz 是元数据库，**不记录播放量/榜单**（其定位是发行与录音元数据），因此不能作为热度来源。

**实现要点**：
1. 搜索 B 站视频接口，按关键字取"最高播放量"；
2. **合集降权**：标题含"合集/串烧/大全/精选/连播/催眠"的视频，其播放量属于合集而非单曲，权重 ×0.15；
3. **标题不含歌名**的视频权重 ×0.2（多半无关）；
4. **歌名短于 3 字判无效**（如"小"、"打"、"祝"极易匹配到无关高播放视频）；
5. **退避重试**：接口限流（HTTP 412）时按 3/6/9 秒退避重试最多 4 次。

**效果**：Top 30 中真实儿歌/名曲占多数（小跳蛙 1732 万、Lemon 1293 万、小星星 492 万、数鸭子 331 万、大风车 158 万等），噪声大幅减少。

### 6.3 批量统计

对 395 张谱（热度序）执行批量转写：

| 项 | 数值 |
|---|---|
| 转写完成 | **395/395** |
| 有效（> 0 音）| **296** 张 |
| 空白（非简谱/格式特殊）| 71 张 |
| 跳过（超长图）| 29 张 |
| 失败 | 12 张 |
| **总音符** | **54,507** |
| 平均每首 | 184 音（中位 166）|
| **双高八度残留** | **0** |
| **`x` 念白记号** | **1985** 个 |

### 6.4 转换为 jianpu-db 格式

输出遵循 jianpu-db 的曲谱格式：

```
%<文件名>.txt
MBID=<MusicBrainz 标识>        (可选)
Wikidata=<Wikidata QID>        (可选)
title=<曲名>
type=work
tag=
usertag=
tagroute=
transcriber=jianpu2-auto(Qwen3-VL-2B)
%--
4/4
subtitle=score
<token 序列>
%END
```

**三处格式对齐**：

1. **高八度位置**：本管线输出 `'1`（撇在前），而 jianpu-ly 规范是 **`1'`**（撇在后）。转换时统一后置。
2. **文件名乱码还原**：爬虫保存文件名时把 UTF-8 字节按 Latin-1 处理，产生 `æ£ä¹çº¢å¸` 之类的 mojibake。转换时按 `encode('latin-1').decode('utf-8')` 还原为中文（`正义红师`）。
3. **拍号自动识别**：用 Qwen3-VL-2B 读取谱头区域，输出拍号。实测：spring → `2/4` ✓，兄弟抱一下 → `4/4` ✓。全量识别分布为 `4/4` 174 首、`2/4` 84 首、`3/4` 19 首、`6/8` 4 首。

### 6.5 关于外部标识（MBID / Wikidata）的决策

本文最初尝试为每首曲目自动查询 MusicBrainz MBID，实测**命中率仅 11%**，且限流严重（失败需重试 9 秒），**86% 的查询是白费**。

原因已查明：**MusicBrainz 是西方社区主导的数据库**，且要求"可验证的发行记录"才能建立作品条目；而中文儿歌/民歌多为口传，**无正式国际发行**，因此建不了条目。验证如下：

| 查询 | MusicBrainz 返回（均为无关项）|
|---|---|
| 数鸭子 | `死鴨子`、`冲鸭冲鸭` |
| 虫儿飞 | `蟲`、`風兒雨兒` |
| 小跳蛙 | `蛙`、`跳`、`青蛙` |

改用 **Wikidata** 后覆盖略有改善（如 `数鸭子 → Q16294665`、`两只老虎 → Q825728 (Frère Jacques)`、`让我们荡起双桨 → Q15951705`），但整体命中仍低。

**最终决策**：**外部标识不是必选项**。查不到就**不写**该字段（而非写空值），需要时后续补充。这也成为 jianpu-db 架构调整的前提（见 §7.1）。

---

## 7. 上游数据集（jianpu-db）的架构改动

### 7.1 标识体系：从"单一主键"到"无主键"

**原设计**：曲谱文件必须含 `MBID=`，解析脚本以 `mbid` 作为 `data.json` 的字典键。

**问题**：
1. 中文曲目普遍无 MBID → 大面积空缺；
2. `data.json` 以空字符串为键时，**多个曲目会互相覆盖**（`b.update({i.mbid: ...})`），只剩最后一条。

**改动**（经历三轮迭代后定型）：
1. 曾引入 `ID=` + `id-type=` 作为通用主键；
2. 最终**彻底取消主键与类型字段**，改为**多个外部标识各自独立、可并存**：

```
%th01_01.txt
MBID=52e3b2d1-80ff-35a7-b11f-f9342c08f22d
title=A Sacred Lot
```

3. `data.json` 的键改为**文件名**（不依赖任何主键）；
4. 索引目录由 `by_mbid/` 扩展为 `by_mbid/`、`by_wikidata/`。

**兼容性**：`MBID=` 与 `Wikidata=` 均可单独出现、也可同时出现；**都不出现也合法**。

### 7.2 曲目状态标记

**需求**：README 中用 🟥 标注"有旋律但**待整理**"、用其它 emoji（☯️/🐢）标注"已整理"。**待整理的曲目不应进入训练数据集**。

**实现**：解析 README 的 emoji 标记，为对应文件写入显式状态字段：

```
status=ok      # 已整理，可进数据集
status=draft   # 待整理，排除
```

`db_to_jsonl.py` 生成数据集时跳过 `status=draft`。

**效果**：首轮生成 `data.jsonl` 时，**25 首已整理曲目进入数据集，284 首待整理被排除**。

**踩坑记录**：emoji 可能是**多码位**（如 ☯️ = U+262F + U+FE0F）。初次实现用正则 `\[(.)\]` 只匹配单字符，导致 th01 系列（使用 ☯️）的 15 首全部漏标。修正为 `\[([^\]]+)\]` 后正常。

### 7.3 `KeepLength`（黏性时值）展开

**背景**：jianpu-db 已有一个 `_expand.txt` 机制，用正则把 `R{n}{...}`（循环）与 `A{...}`（变奏）**展开为线性文本**，便于计算机直接读取。但它**不处理 `KeepLength`**。

**`KeepLength` 语义**：简谱（及 jianpu-ly）允许省略时值——设定 `KeepLength` 后，后续未标注时值的音符**沿用前一个音符的时值**，直到出现新的时值标记。

**查询 jianpu-ly 是否有现成接口**：结论是**有实现、无接口**。

- 实现位于 `jianpu-ly.py` 的 `addNote()`：
  ```python
  if nBeams==None:               # 音符未写时值
      if self.keepLength:
          nBeams = self.lastNBeams    # 继承上一个音符
      else:
          nBeams = 0
  ```
- 但它**没有对外暴露"展开后的 token 序列"**——其输出是 LilyPond/ABC/MIDI。调用其内部状态机的成本高于自行实现。

**本文实现**（`expand_keep_length()`）：按其次序语义重写，约 20 行。

**作用域问题**（由使用者指出）：`KeepLength` 的作用域**到下一个 `subtitle=`（或 `NextScore`）即失效**。初版实现未重置状态，会导致后续段落被错误补全。修正为在 `subtitle=` / `NextScore` 处重置 `keep` 与当前时值。

**验证**：

输入：
```
%t
%--
KeepLength
s1 1 1 1 c1 2 3 q4 5
```
输出：
```
s1 s1 s1 s1 c1 c2 c3 q4 q5
```
（`s1` 设定十六分 → 后续 `1 1 1` 补 `s`；`c1` 改为四分 → `2 3` 补 `c`；`q4` 再改 → `5` 补 `q`）

真实曲谱（th01_01_expand）验证：
```
subtitle=intro
KeepLength
,6s s3 s,6 s2 s1 s,7 ...        ← 生效（全带 s）
subtitle=layer
6 - 7 - 1' - - - 7 - 2' ~ q2'   ← 已重置，无补全 ✓
subtitle=chorus
KeepLength
0q q0 q0 q0 q0 q3 q6 q7 ...      ← 新段生效 ✓
```

### 7.4 修复的两个上游 bug

**(a) `write_buf()` 把单值字段拆成字符列表**

```python
print(i + '=' + (',').join(self.others[i]), file=f)   # 假设 others[i] 一定是列表
```

`mbid` / `wikidata` 是**单值字符串**，于是 `','.join('52e3b2d1-...')` 把 UUID 逐字符拆开并用逗号连接，写成 `5,2,e,3,b,2,d,1,...`。

**更严重的是**：`parse()` 的最后一步是 `move_buf()`，它会**用生成的 buf 文件覆盖原始曲谱文件**。因此这个格式错误会**落盘污染源文件**，且**每跑一次就多 join 一次**（第二次变成 `5,,,2,,,e,,,3`）。

**修复**：
```python
_v = self.others[i]
print(i + '=' + ((',').join(_v) if isinstance(_v, list) else str(_v)), file=f)
```

**(b) 相似的错误在迁移脚本中重演**

本文为迁移 `MBID=` → `ID=` 所写的脚本也曾把 UUID 拆坏。**修复方式是在脚本中加入格式校验**（只迁移合法 UUID，非法值原样保留），并增加迁移后的**自检**步骤。

这两处教训一致：**在 Python 中，字符串与字符列表在 `join` 面前极易混淆**，凡涉及标识符的读写都必须做类型与格式校验。

---

## 8. 实验结果

### 8.1 单谱回归（spring《春天在哪里》）

| 阶段 | 音数 | OK |
|---|---|---|
| Qwen2.5 六头基线 | — | 83 |
| 组合管线（初版）| 142 | 85 |
| + 共同下划线修复 | 142 | 86 |
| + 碎片聚类 | 142 | **87** |
| + 延音杠修复（中心点判定）| **113** | **87** |
| GT | 114 | — |

**注**：延音杠修复后音数由 142 降至 113，**与 GT 的 114 几乎完全一致**——说明此前多出的 28 个 token 全是假延音。

### 8.2 多谱验证

| 谱 | 音数 | GT | OK | 命中率 | 评估 |
|---|---|---|---|---|---|
| 春天在哪里 | 113 | 114 | **87** | **76%** | ✅ 可部署 |
| 排排坐 | 26 | 31 | **25** | **81%** | ✅ 可部署 |
| 时间都去哪了 | 173 | 153 | 55 | 36% | ⚠️ 多切 |
| 兄弟抱一下 | 248 | 192 | 23 | 12% | ❌ 多切 |

### 8.3 失败案例的机制分析

"时间都去哪了"与"兄弟抱一下"的转写音符数**远超** GT（173 vs 153、248 vs 192），且排除了"假延音"这一可能（已修复）。

**根因定位**：这两张谱面的**音符行与下方歌词行贴合过紧**，行切分未能把它们分开（检测到的行高约 116 像素，而正常音符行约 40 像素）。结果是**歌词汉字被当作音符块**送进了识别器，产生大量伪 token。

**尝试过但无效的修法**（均已回退）：
- 提高小节线细高门槛 → 真小节线因"与歌词连通"（宽 > 5）被排除，反而更糟；
- 缩小行间隙 `ROW_GAP`（8 → 4）→ 音符行被跳过、歌词行反被当音符行；
- 限制附属符号距离（数字底 + 20 像素）→ spring 退化（87 → 79）。

**正确方向（未实施）**：在行内做 **y 方向切分**——依据暗像素投影的低谷把"音符行"与"歌词行"切开。这是一个独立且工作量可观的子问题。

### 8.4 效率分析：时间到底花在哪

对单张谱（约 180 个音符块）的耗时做分解：

| 阶段 | 占比 | 说明 |
|---|---|---|
| **图像二值化与行切分** | < 5% | 纯 numpy 投影运算，毫秒级 |
| **连通域分析与块聚类** | < 10% | 取决于图像尺寸，通常在秒级 |
| **视觉编码（ViT 前向）** | **约 70–80%** | 每个音符块一张小图，逐块过视觉塔 |
| **文本生成** | 约 10% | 每个块生成 1–2 个 token |
| **几何判定与组装** | < 5% | 纯规则运算 |

**结论：瓶颈是视觉编码，且与"识别多少个块"成正比。**

由此可以解释 §5.5 中三项加速尝试为何全部失败：

- **缓存去重**失败，是因为不同音符块的图案（数字形状 + 下划线位置 + 尺寸）**实际并不重复**，命中率极低，而指纹计算本身有成本；
- **降采样**失败，是因为 processor 会把小图重新放大到 `min_pixels` 预算——patch 数量由预算决定而非输入尺寸，**缩小输入等于白做**；
- **降低像素预算**失败，是因为 Qwen3-VL 的 processor 不接受该属性（设置被静默忽略），实测耗时反而因重复尝试略增。

**唯一有效的手段是"并行"**：批量前向（一次 8 块）让多个块的 ViT 计算同时进行，缩短了 GPU 的等待间隙，获得约 25% 提升。再加批量收益递减，因为总计算量不变而 GPU 已接近饱和。

**多进程为什么无益**：本机只有一张 GPU。多个进程只会竞争同一块显存与计算单元，且各自还要加载一份 4 GB 的模型权重，实际表现为更慢。若有多卡，才谈得上数据并行。

### 8.5 数据质量抽样

对批量产出做分层抽样检查，得到如下定性结论：

**质量良好的谱面特征**：
- 音符行与歌词行之间有清晰空白；
- 扫描分辨率足够（音符高度 ≥ 25 像素）；
- 无严重倾斜或透视变形；
- 时值下划线连续（未断裂成大量碎片）。

这类谱面的转写结果**人工核对时基本可读**，错误集中在两类：一是相邻同名音符的下划线归属（`q3 q3` 与 `s3 s3` 之争），二是密集十六分音符处的时值判定。

**质量较差的谱面特征**：
- 音符行与歌词粘连（§8.3）；
- 图像长宽比异常（超长条，§4.2）；
- 手机拍摄的倾斜照片（行投影出现斜坡，切分错位）；
- 复调记谱（同一行两组声部，简谱中以两排数字并列表示）——本管线目前按"单声部"假设处理，会交错提取。

**关于复调**：简谱中的合唱/双声部记谱通常把两个声部上下排列在同一行内，这在几何上与本管线"一行 = 一个声部"的假设冲突。正确做法是在行内按 y 做二次分组，把上行声部与下行声部分开。这是与 §8.3 的"音符/歌词切分"同源的问题，可一并解决。

### 8.6 与手工转写的对比

以《春天在哪里》为例，人工转写（GT）为 114 个 token，本管线输出 113 个，OK=87（76%）。逐类统计差异来源：

| 差异类型 | 估计占比 | 典型例子 |
|---|---|---|
| **时值判定错误** | 约 40% | 十六分被读成八分（下划线断裂）|
| **八度遗漏/多余** | 约 25% | 低八度点与歌词笔画混淆 |
| **漏音符** | 约 20% | 极窄的音符（如 `1` 与 `7` 紧贴）被合并 |
| **多音符** | 约 15% | 附点或装饰点被当作独立音符 |

值得注意的是：**这些差异都不改变旋律的可读性**。对"用简谱记录旋律"这一实际目标而言，76% 的 token 精确率已经使输出"大体可用"，但距离"可直接出版"仍有差距。

---

## 9. 局限

1. **复杂谱面**：如 §8.3 所述，"音符行与歌词行粘连"的谱面转写命中率仅 12%–36%。
2. **空白转写**：本次批量中 71 张转出 0 音（非简谱、纯歌词页、或五线谱），占比约 18%。
3. **超长图**：多页拼接的长条图被跳过（29 张），它们需要按页拆分为独立曲目。
4. **外部标识**：中文曲目普遍无法自动获得 MBID，Wikidata 也仅部分覆盖。
5. **速度**：单张谱约 80–110 秒；受限于"每个音符块都要过一次视觉编码"这一模型架构特性，在当前单卡条件下已接近该方案上限。
6. **调号与音域**：转写输出为**首调记法**，未提取调号（`1=E` 之类），故不能直接还原绝对音高。

---

## 10. 未来工作

1. **行内 y 切分**：解决"音符行/歌词行粘连"，预期可显著改善复杂谱面的表现。
2. **调号与拍号提取**：拍号已可自动识别，调号提取可进一步提升数据的可用性。
3. **小节线感知**：保留小节线位置，使输出更接近完整的记谱语义。
4. **多页长条图自动分页为独立曲目**：把 `skipped.txt` 中的 29 张图按页拆分后纳入流程。
5. **人工校对闭环**：以可视化标注图为界面，支持"转写—人工修正—回流训练/评测"。
6. **更小的模型或蒸馏**：若能将识别器进一步小型化，可显著降低成本。

---

## 11. 结论

本文实现了一套可用的简谱图像转写管线，并以此完成了 4007 张谱图的批量处理，产出：

- **367 套转写结果**（296 有效，**54,507 个音符**）及其可视化标注图；
- **281 首**符合 jianpu-db 格式的曲谱（含自动拍号识别）；
- **HuggingFace 合规的 JSONL 数据集**（仅含已整理曲目）；
- 对上游数据集 jianpu-db 的**架构改进**（标识体系、状态标记、`KeepLength` 展开）与**两个 bug 修复**。

在方法层面，本文验证了"**几何切分 + 小模型逐块识别**"这一组合架构的有效性：它避开了端到端生成与特征回归的失败（§5.1–5.3），把识别任务约束到 2B 模型足以胜任的粒度，并通过一系列**针对性的几何判定修复**（§3）达到可用精度。

同时，本文如实记录了七项失败方案与多项被回退的参数改动。对于这类"几何规则 + 模型识别"的混合系统，**每一次判定的放宽都可能在其他位置引入退化**，因此**以单一基准谱做回归验证**是必要的工程纪律。

---

## 附录 A：产出清单

| 路径 | 内容 |
|---|---|
| `tools/jp_transcribe.py` | 转写主模块（切分 + 识别 + 判定 + 组装 + 出图）|
| `tools/transcribe.py` | 几何切分（行切分、块聚类）|
| `tools/transcribe_qwen.py` | 视觉编码与音符行约束 |
| `tools/geo_detect.py` | 时值/八度/附点几何判定 |
| `tools/classify_block.py` | 块分类（digit/dash/rest）|
| `tools/batch_transcribe.py` | 批量转写（热度序、谱页选择、分页、过滤、断点续传）|
| `tools/rank_by_bili.py` | B 站播放量热度排序 |
| `tools/to_jianpu_db.py` | jianpu-db 格式转换（拍号、标识、mojibake 还原）|
| `tools/db_to_jsonl.py` | HuggingFace JSONL 生成 |
| `tools/db_progress.py` | 数据集转写进度统计 |
| `batch-out/` | 367 套转写结果（txt + 标注图）|
| `jianpu-db-out/scores/` | 281 首 jianpu-db 格式曲谱 |
| `rank-out/` | 热度排序结果 |
| `README_jp_v2.md` | 部署与使用文档 |

## 附录 B：关键参数

| 参数 | 值 | 说明 |
|---|---|---|
| `ROW_GAP` | 8 | 行切分间隙阈值 |
| `TOL` | 依图像 | 二值化阈值 |
| `BEAM_PRE` | `{0:"",1:"q",2:"s",3:"d",4:"h"}` | 时值前缀映射 |
| `voice` 上限 | 1 | 高八度点数上限 |
| `low` 上限 | 3 | 低八度点数上限 |
| `JP_BATCH` | 8 | 批量识别大小 |
| `JP_MAX_H` | 3000 | 超长图跳过阈值（像素）|
| 延音杠宽度判据 | 中心点不落在任何数字 x 范围内 | 区别于时值下划线 |
| 时值线判据 | 同行碎片总宽 ≥ 4 像素 | 容忍扫描断线 |

---

## 附录 C：核心算法伪代码

### C.1 主转写流程

```
function transcribe(image):
    gray    ← 灰度化(image)
    binary  ← gray < TOL
    rows    ← fine_rows(binary, ROW_GAP)            # 水平投影切行
    blocks  ← []

    for (y0, y1) in rows:
        sub ← binary[y0 : y1+1]
        if count_bars(sub) < BAR_THR:                # 非音符行(标题/歌词)
            continue
        be ← bar_extent(sub)                         # 小节线范围, 用于纵向约束
        for (x0, x1, ny0, ny1) in crop_note_regions(sub):
            crop ← bound_to_note_row(gray[y0:y1+1], x0, x1, ny0, ny1, be)
            if crop is empty: continue
            blocks.append( classify_block(crop), crop, (y0, x0, x1, ny0, ny1) )

    digits ← batch_recognize( [c for (t,c,_) in blocks if t == "digit"] )   # 一次前向 8 块

    tokens ← []
    for i, (btype, crop, pos) in enumerate(blocks):
        if btype == "dash":   tokens.append("-")
        elif btype == "rest": tokens.append("0")
        elif btype == "digit":
            d  ← digits[i]                            # 1-7 / 0 / x
            gd ← geo_detect(crop)                     # beam / low / voice / dotted
            if d == "0":
                tokens.append( BEAM_PRE[gd.beam] + "0" )        # 休止符: 有时值, 无八度/附点
            else:
                tokens.append( BEAM_PRE[gd.beam]
                               + "," * gd.low
                               + "'" * gd.voice
                               + d
                               + "." * gd.dotted )
    return tokens
```

### C.2 独立延音杠的判定

```
function is_tie_dash(comp, digits):
    # 形状: 短而扁的横线
    if not (comp.w > 10 and comp.h <= 4 and comp.w > 2 * comp.h): return false
    # 关键: 中心点不得落在任何数字的 x 范围内
    #   —— 落在数字内的是"时值下划线", 属于数字, 不能独立成块
    dc ← (comp.x0 + comp.x1) / 2
    for d in digits:
        if d.x0 <= dc <= d.x1: return false
    return true
```

### C.3 时值线计数（容忍断线）

```
function count_beams(crop, digit_box):
    frags ← [c for c in components(crop)
             if c is not digit_box
             and c.h <= 3                # 细
             and c.w >= 2
             and c.w / max(c.h,1) >= 2   # 扁
             and c.y0 > digit_box.y1 - 2 # 位于数字下方
             and c.x0 < digit_box.x1]
    groups ← cluster_by_y(frags, tol = 2)         # 扫描图中一条线会断成多段
    beam ← 0
    for g in groups:
        if sum(c.w for c in g) >= 4:              # 该行碎片总宽达标才算一条
            beam += 1
    return min(beam, 4)
```

### C.4 `KeepLength` 展开

```
function expand_keep_length(text):
    cur_val ← ""          # 当前生效时值
    keep    ← false       # KeepLength 是否生效
    for line in text.lines:
        if line == "KeepLength": keep ← true; emit(line); continue
        # 作用域: 换 subtitle / NextScore 即失效
        if line.startswith("subtitle=") or line.lower() == "nextscore":
            keep ← false; cur_val ← ""; emit(line); continue
        if line is blank or line.startswith("%"): emit(line); continue
        out ← []
        for tok in line.split():
            val ← extract_duration(tok)      # 前置(q1/s,6) 或 后置(1q/,6s/3c.)
            if val != "":
                cur_val ← val; out.append(tok)
            else:
                out.append(cur_val + tok if (keep and cur_val) else tok)
        emit(join(out))
```

---

## 附录 D：回归测试用例

以下用例用于验证各项修复，均可独立运行。

| 编号 | 用例 | 期望 |
|---|---|---|
| **T1** | 《THERE IS A REASON》整谱 | `-` 占比 ≤ 5%（修复前为满屏）|
| **T2** | 《春天在哪里》| 音符数 ≈ 114（GT），OK = 87 |
| **T3** | 《红星歌》（曾含 99 个 `''`）| `''` = 0，音符数 518 |
| **T4** | 《2026年欢乐笑开颜》（曾含 83 个 `''`）| `''` = 0，音符数 534 |
| **T5** | 《Together We Fight》| 含 `x` token ≈ 27 个 |
| **T6** | 含 `3 - - 3` 的片段 | 两个 `-` 都有框，不被吞 |
| **T7** | 含共用下划线的 `6 1 2 2` | 下划线不独立成块 |
| **T8** | `expand_keep_length("KeepLength\ns1 1 1 1 c1 2 3 q4 5")` | `s1 s1 s1 s1 c1 c2 c3 q4 q5` |
| **T9** | `KeepLength` + `subtitle=` 跨段 | 换段后不补全 |
| **T10** | qupu123 目录（`001.jpg` 为标题条）| 取最大 jpg 为谱页，非 0 音 |
| **T11** | RGBA 图 | 保存标注图不报错 |
| **T12** | 高 7922 像素长条图 | 按行边界切页，逐页转写 |

---

## 附录 E：术语表

| 术语 | 含义 |
|---|---|
| **简谱** | 以数字 1–7 记音级的记谱法 |
| **音符块（block）** | 一个数字及其附属符号（下划线/八度点/附点）构成的整体 |
| **时值下划线** | 数字下方的横线，数量表示时值（1 条 = 八分，2 条 = 十六分……）|
| **延音杠** | 表示延长前音时值的独立短横线，**不与数字 x 重叠** |
| **共同下划线** | 多个连续音符共享的一条长下划线（简谱的常见省略写法）|
| **念白记号 `x`** | 表示无固定音高（念白/打击/喊声）的记号 |
| **首调记法** | 以调式主音为 `1` 的记法；同一旋律换调后数字不变 |
| **`KeepLength`** | jianpu-ly 指令：后续未标注时值的音符沿用前一个的时值 |
| **expand 版** | 把 `R{}/A{}` 循环与 `KeepLength` 展开后的线性曲谱文本 |
| **OK 值** | 与人工标注序列做 `SequenceMatcher` 后"相等片段长度之和"|
| **热度序** | 按 B 站播放量降序排列的转写优先级 |
| **mojibake** | 编码错乱（如 UTF-8 字节被按 Latin-1 解读）|

---

## 附录 F：关于"两次字符串被拆坏"的复盘

本项目调试中最耗时的一个坑，值得单独记录，因为它属于"看起来不可能发生"的类型。

**现象**：`scores/th01_01.txt` 中的 `MBID=52e3b2d1-...` 变成了 `ID=5,,,2,,,e,,,3,...`。

**排查过程**：

1. **第一反应是"我的迁移脚本写坏了"**——检查后发现脚本逻辑正确；
2. **用 git diff 定位**：确认原文件是 `MBID=52e3b2d1-...`，当前是坏值，说明**确实被某次写入破坏**；
3. **从"坏值的形状"反推**：`5,,,2,,,e` 的特征是两个逗号之间夹一个字符。这只能由 `','.join()` 作用于**字符串**（而非列表）产生：`','.join('52e3')` = `'5,2,e,3'`，再一次 `','.join('5,2,e,3')` = `'5,,,2,,,e,,,3'`。**两次 join** 的痕迹说明文件被处理了两轮。
4. **找到第一个凶手**：`write_buf()` 中的 `(',').join(self.others[i])`——它假设所有 `others` 值都是列表，而 `mbid` 是单值字符串。
5. **发现污染路径**：`parse()` 的最后一步 `move_buf()` 会用 buf 文件**覆盖源曲谱文件**，因此格式错误会落盘，且每跑一次就多 join 一次。
6. **修复后仍有残迹**：因为源文件已被污染，直接重跑只会把坏值再 join 一次。必须先用 git 恢复源文件、清掉 `_buf.txt` 残留，再重跑。

**教训**：

- Python 中 `str` 与 `list[str]` 在 `join` 面前极易混淆，**凡是读写标识符的代码都应做类型与格式校验**；
- 一个会**写回源文件**的解析流程（`parse()` → `move_buf()`），其内部任何格式错误都是**破坏性**的，调试时应先做备份（本项目正是靠 git 才得以恢复）；
- 迁移类脚本**必须带自检**（本项目最终版本的迁移脚本会校验"所有 `ID=` 值要么是合法 UUID，要么是合法 QID，要么为空"）。

---

*报告完*
