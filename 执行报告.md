# 简谱图片 → jianpu-db 批量转换：执行报告

日期: 2026-08-22 | 模型: qwen2.5vl:3b (本地 Ollama) | 目标格式: jianpu-db

## 一、执行结果统计

| 数据集 | 来源分类 | 曲数 | 草稿 .txt | 通过 jianpu_ly 校验 |
|---|---|---|---|---|
| images/ready | 三字歌谱 | 30 | 29 | 0 |
| images/ready2 | 四字歌谱 | 30 | 28 | 0 |
| images/ready3 | 五字歌谱 | 30 | 26 | 0 |
| images/ready4 | 合唱谱 | 30 | 29 | 1 |
| images/ready5 | 英文歌谱 | 30 | 30 | 3 |
| images/test-jianpucn | 三字(旧) | 30 | 30 | 0 |
| images/test-jianpujia | 简谱之家-儿歌 | 100 | 94 | 0 |
| **合计** | | **280** | **266** | **4** |

- 含 ≥8 个音符的草稿: **265/280** (有实质内容, 但夹杂幻觉/循环片段)
- 空/全废: ~14 首 (扫描质量过差或模型完全拒绝)

## 二、质量评估（如实）

**结论: 草稿级, 未达 jianpu-db 入库标准。** 典型输出示例 (小星星, 真实旋律应为
1 1 5 5 6 6 5 - 4 4 3 3 2 2 1 -):

```
4 4 2 - 1 0 0
| 3 | 5 - |
5 4 | 3 2 - | 5 4 |
```

- ✅ 能读出: 数字音符、附点、延长线、高低音(i→'1 已映射)、小节线、调号(部分)、拍号(部分)
- ❌ 问题: 音符顺序错乱/漏读、幻觉音阶、长横线循环、部分歌整首废

**根因**: qwen2.5vl:3b 对老网站低质量扫描图的 OCR 能力有限; 理想模型是 7b。

## 三、7b 模型下载解法 (质量提升的关键)

`ollama pull qwen2.5vl:7b` 今晚反复卡死在 ~95% (网络对长连接限速), 且有 ollama
0.32.15 的 Windows 分块清理 bug。尝试顺序:

1. **重启 Ollama 后重试**: 退出任务栏 Ollama → 重开 → `ollama pull qwen2.5vl:7b`
   (断点续传, 每次重启都有机会补完; 3b 就是用此法补完最后 150MB)
2. **挂代理**: 系统代理开启后 ollama 自动走 HTTP(S)_PROXY
3. **用 3b 顶替**: `--model qwen2.5vl:3b`, 或 granite3.2-vision:2b (应急, 质量更差)

## 四、使用方式

```bash
# 1. 爬取 (已完成的 280 首在 images/ 与 images-prep/)
python crawler.py jianpucn --cat sanzigepu --pages 3 --out images/xxx

# 2. 预处理 (缩放+切片, 视觉模型必需)
powershell -ExecutionPolicy Bypass -File tools/preprocess.ps1 -Source images/xxx -Dest images-prep/xxx -Strips 240

# 3. 转换 (jianpu-db 格式输出)
python convert.py --input images-prep/xxx --out scores-xxx --model qwen2.5vl:3b
# 推荐: --transcriber 你的ID --tags 分类 --lookup-mbid
```

输出: `<歌名>.txt` (jianpu-db 格式) + `.trans` (模型原文+逐条明细) + `.err` (失败原因) + `.ly` (校验通过时)

## 五、已知问题与建议

1. **模型退化**: 连续推理 ~1h 后输出变 `@@@...` 或挂起 — convert.py 已内置自愈
   (空输出 → 自动重启 ollama 服务器+llama-server → 重试该首)
2. **草稿补救路径**: 用 `.trans` 里的"逐条明细"人工修正 `.txt`, 或等 7b 后
   `python convert.py` 重跑 (已有 .txt 的会自动跳过, 先删掉要重做的)
3. **格式兼容**: 输出已用 `parse_scores.py` 实测通过 (MBID/标题/展开文件均正常)
4. `i→'1` 映射按用户指示实现 (jianpu-ly 同时接受 `'1` 与 `1'`; 你库中现有文件用 `1'`)
