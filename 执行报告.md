# 简谱图片 → jianpu-db 批量转换：执行报告（7b 最终版）

日期: 2026-08-22~23 | 模型: qwen2.5vl:7b (本地 Ollama, 整图单次转写) | 目标格式: jianpu-db

## 一、数据规模

| 数据集 | 来源分类 | 曲数 |
|---|---|---|
| images/ready ~ ready5 | jianpu.cn (三字/四字/五字/合唱/英文) | 150 |
| images/test-jianpucn | jianpu.cn (三字, 旧) | 30 |
| images/test-jianpujia | jianpujia.com (儿歌) | 100 |
| **合计** | | **280** |

## 二、转换结果（qwen2.5vl:7b 整图单次）

| 数据集 | 输出 | jianpu-ly 校验通过 | score.py 兼容 |
|---|---|---|---|
| scores-7b | 29 | 29 | 29/29 |
| scores-7b-2 | 28 | 28 | 28/28 |
| scores-7b-3 | 26 | 26 | 26/26 |
| scores-7b-4 | 29 | 29 | 29/29 |
| scores-7b-5 | 30 | 30 | 30/30 |
| scores-7b-pucn | 29 | 29 | 29/29 |
| scores-7b-pujia | 95 | 95 | 95/95 |
| **合计** | **266** | **266 (100%)** | **266/266** |

- 280 首全部有输出；14 首因清洗后标题重名合并（同名只保留一份，见 import-bundle 清单）
- **零 .err**：所有失败歌曲经"失败重跑"（tools/rerun_errs.py）与最终版管线全部通过
- 每首含：`*.txt`（jianpu-db 格式，已后处理 `%TODO:revise` + `usertag=to_be_revised` +
  `copyright=谱源站点,版权归原作者及原网站`）、
  `*.ly`（jianpu-ly 渲染）、`*.trans`（模型原文）、`summary.csv`、`mbid_review.csv`
- MBID 补录结果见各目录 `mbid_review.csv`（填录数量见文末统计）

## 三、质量评估（如实）

**结论: 草稿级 → 格式完全合规, 旋律内容大部分可读, 仍建议人工修订。**
相比 3b 草稿（266 首仅 4 首过校验），7b 的改进：

- ✅ **格式 100% 合规**：`能不报错就是对的格式` —— 266/266 通过 jianpu-ly 校验、
  score.py 全量 parse 通过、可被 jianpu-db 流水线正常消费
- ✅ **识别质量明显提升**：音符顺序、时值（q/s/d 前缀）、高八度（1'）、
  低八度（,6）、升降号（#4/b7）基本正确；结构行（subtitle/NextScore/R{}/A{}）
  模型已能部分输出并被管线规整
- ⚠️ 仍为草稿：少量幻觉/漏读/循环片段、小节线不齐（管线自动补齐休止）、
  歌词混入 subtitle、重复结构残缺 —— 全部已打 `to_be_revised` 标记

**典型输出**（意难平，已校验通过）:

```
title=意难平
type=work
%TODO:revise
usertag=to_be_revised
MBID=c1610610-e0b2-410b-ae87-6d4ee760010b
copyright=歌谱简谱网,版权归原作者及原网站
%--
4/4
0 - 0 0 | 3 - 2 0 | 6 - 5 0 | 7 1' 1' q1. s0 | 'q1. 0 0 0 s0 | 0 0 0 0 |
...
%END
```

## 四、管线健壮性（本次开发的重点）

`convert.py` 内置的自动规范化（全部 266 首零 .err 的基础）：

| 规则 | 说明 |
|---|---|
| 八度记号 | 高八度撇号统一在数字后（`1'`），低八度逗号统一在数字前（`,6`），与语料一致 |
| 唱名误读 | `i`/`I` → `1'`；`8`/`9`（jianpu-ly 内置高音 1/2）→ `1'`/`2'` |
| 粘连延音线 | `2-` → `2 -`（jianpu-ly 把 `2-` 当和弦=1拍；语料规范是独立 `-`） |
| 小节补全 | 每小节补休止到整拍（1/64 精度）；无小节线行按拍号自动分小节；超拍小节自动重分 |
| 拍号分段 | 每个 NextScore 分段都补拍号（jianpu-ly 各 score 独立缺省 4/4） |
| 结构行清洗 | `R{ ... }` 省略号、`###`、`#10`、`A{|`、`|}`、孤立 `}`、空段等自动剔除/拆开 |
| 重复结构 | R{}/A{} 块内按拍补全；残缺结构压平为普通音符行（音符保命） |
| 结尾清理 | 末尾 NextScore 及无音符段落（如"前奏"空段）自动删除 |
| 自愈重试 | 超时/只有歌词/上下文不足 → 自动重启 Ollama 服务器重试一次 |

回归测试：`tools/regression.py` 覆盖 28 个历史失败样本，全部通过。

## 五、使用方式

```bash
# 1. 爬取 (已完成 280 首)
python crawler.py jianpucn --cat sanzigepu --pages 3 --out images/xxx
python crawler.py jianpujia --list 21436 --pages 2 --out images/xxx

# 2. 预处理
powershell -ExecutionPolicy Bypass -File tools/preprocess.ps1 -Source images/ready -Dest images-prep/ready

# 3. 转换 (7b 整图)
python convert.py --input images-prep/ready --out scores-7b --model qwen2.5vl:7b --no-strips

# 4. 失败重跑 → 后处理 → 校验 → MBID → 导入包
python tools/rerun_errs.py
python tools/postprocess.py
python tools/add_copyright.py        # 补 copyright=谱源行 (值须文件系统安全)
python tools/validate_repo.py
python tools/validate_drafts.py
python convert.py --out scores-7b --mbid-only --input images-prep/ready   # 歌手增强
python tools/make_mbid_map.py      # 生成建议映射 + 人工复核清单
python convert.py --out scores-7b --mbid-only --mbid-file mbid_map_suggested.json
python tools/make_bundle.py
```

## 六、MBID 补录统计（艺术家增强）

两轮补录：`python convert.py --out <目录> --mbid-only --input <图片目录>`（自动从
song.json 提取歌手，分级置信度查找，已填的不重复查询）：

| 目录 | 已填/总数 | 目录 | 已填/总数 |
|---|---|---|---|
| scores-7b | 7/29 | scores-7b-5 | 20/30 |
| scores-7b-2 | 2/28 | scores-7b-pucn | 7/29 |
| scores-7b-3 | 2/26 | scores-7b-pujia | 63/95 |
| scores-7b-4 | 0/29 | **合计** | **101/266 (38%)** |

- 首轮（无歌手）86/266 → 第二轮（带歌手佐证）+13 → 应用建议映射 +2 = **101**
- **建议映射**：`mbid_map_suggested.json`（2 首：意难平/银临、一闪一闪亮晶晶，
  已用 `--mbid-file` 写入文件）—— 生成器 `tools/make_mbid_map.py`
- **人工复核清单**：`mbid_manual_review.csv`（144 首未填 + 候选/近似匹配说明，
  如 小重山→"小重山 LIVE"、悯农→"憫農 Toiling Farmers" 等）
- MusicBrainz 对华语儿歌覆盖有限：167 首未填中仅 10 首有任何候选，
  其余在 MB 库中根本不存在（已如实标注"无候选"）
- 3b 草稿同期补录 ≈91/266，覆盖大体一致

## 七、7b vs 3b 质量对比（tools/compare_models.py 实测）

| 指标 | 3b (scores-draft*) | 7b (scores-7b*) |
|---|---|---|
| 格式通过（jianpu-ly, 含补拍号尝试） | **9/266 (3%)** | **266/266 (100%)** |
| score.py 仓库兼容 | 266/266 | 266/266 |
| 总音符数 | 28,677 | 61,553 (×2.1) |
| 八分/十六分/三十二分时值 | 0 / 0 / 0 | 9,000+ / 1,200+ / 500+ |
| 段落结构 (subtitle/NextScore/R{}) | 0 / 0 / 0 | 有（多首含完整分段） |
| 延音线数量 | 13,000+（大量循环垃圾） | 3,000+（正常记谱） |

结论：3b 输出"音符都在但节奏/结构全丢"（全部扁平化为四分音符、无小节结构、
大量横线循环），仅格式就不合格；7b 输出含真实时值、八度、结构信息，
格式 100% 合规 —— 两者差距是模型能力决定的，不是管线差异。

## 八、已知问题与建议

1. **重复标题合并**：爬虫含同名歌曲（如两首"小螺号"），清洗后文件名冲突，
   后写覆盖先写 —— 导入包时同名只留一份，建议人工核对后补录另一首
2. **模型偶发退化**：连续推理 ~1h 后可能超时/输出歌词 —— 管线已自动重启重试；
   若个别歌仍失败，删掉对应 .txt/.err 后重跑即可
3. **草稿修订**：`.trans` 里有模型原文，`tools/validate_drafts.py` 会指出未过格式的
   文件；修订后把 `usertag=to_be_revised` 与 `%TODO:revise` 移除即可入库
4. **MBID 人工补录**：看 `mbid_manual_review.csv` 的 candidates 列挑对后，
   写进 `mbid_map_suggested.json` 再跑 `--mbid-file`；MB 库没有的曲目
   可考虑在 jianpu-db 侧用 work 实体手工创建后回填
