# 简谱 OCR 模型微调报告（QLoRA · Qwen2.5-VL-3B）

日期: 2026-08-24 | 数据: 合成谱面 (图像→jianpu-ly 文本) | 框架: transformers 4.57 + PEFT + bitsandbytes

## 一、数据管线（用户建议的随机生成路线）

- `tools/synth_scores.py`：随机生成 **600 首 jianpu-ly 合法谱面**，**通过 jianpu_ly.process_input 验证**（合法率 97-99%）
- 语法特征覆盖：9 种拍号、大小调调号、速度、OctavesBefore/After、KeepLength、NextScore 分段、subtitle、三连音、前倚音、倚音和弦 g[1&3&5]、普通和弦连写、圆滑线、8/9 快捷记号、升降号、高低八度、休止、延音线
- `tools/render_ly.py`：jianpu-ly → LilyPond → **600/600 PDF 渲染**（v3 起 beam 加粗 0.4/0.7，解决 s 双线糊成 q 单线）
- `tools/pdf_to_png.py`：PyMuPDF → **600 张 150dpi PNG**（A4 单页）
- `tools/augment.py`：扫描模拟增强（噪点/旋转/模糊/JPEG/纸色/对比度），每张 2 变体共 1200 张
- `train-data*/manifest.jsonl`：(图像, 完整谱面文本) 配对

jianpu-ly 真实语法规则（踩坑记录）：
- 小调调号是 `6=F#`（**不带 m** 后缀）
- `2-`/`0-` 粘连时值会被当和弦 → 延音线必须独立 token
- 普通和弦是**连写**（135），`&` 只用于倚音和弦
- 每个 NextScore 分段要**重写拍号和 Octaves 指令**
- KeepLength 下裸数字/`-` 会继承时值 → 只用显式时值
- **时值字母：q=八分(quaver)、s=十六分(semiquaver)、c=四分(crotchet)、d=三十二分、h=六十四分；裸数字=四分**
- **R{...} 糊一行会教坏"音符逐一对应"**（用户核心要求：音符逐个对应图片，不省略不合并）→ v5 起生成器去掉 R{}

## 二、训练版本史

| 版本 | 数据 | 关键改动 | 结果 |
|---|---|---|---|
| v1 | train-data 600 | 首版 | 合成 F1 0.97 |
| v2 | train-data-aug 600+1200 | 扫描增强 | 合成 F1 0.96 |
| v3 | 未完成 | 中途废弃 | - |
| **v4** | 600+1200，八度均衡 45% | 全特征 | **真实图：低八度全丢、q 幻觉 17 处（小星星）** |
| **v5** | train-data-v2 600+1200，时值分布修正 | 裸四分 13.3%→41.9%、h 19.1%→5%、去 R{}、prompt 内容导向 | **q 幻觉 17→1、低八度 0→5/7 命中、歌词 L: 分离；上海人 19/35 同位置（音高 35/35）** |
| **v6** | train-data-v3 600+1200，beam 加粗 + s 权重翻倍 | s 14%→20%、附点权重提升 | **上海人 22/35 同位置、低八度 7/7 全命中、q 幻觉 0（小星星）** |
| **v7/v7b** | 原子 1479 + 整曲（原子化实验，用户思路） | 孤立单音符卡片 | **实验结论：卡片教"看字卡"不教排版；回火后部分恢复，v6 仍优** |
| **v8** | train-data-v8 = v3 整曲 600 + 形态覆盖补充 162 | **形态全覆盖**：v3 只覆盖 322/1479 形态（22%），升降/倍八度/双附点等 1157 种从未出现 → 补充曲目把缺失形态内嵌真实排版 | **上海人 24/35（历史最佳）、低八度 7/7 保持** |

## 二点五、原子化训练（用户提出，2026-08-25）

**动机**：v6 的 s/q 时值混淆、低八度方向错误（歌声与微笑 `6, 6'` vs GT `6 6,`）说明整曲级"顺便学"音符形态不可靠——模型靠上下文猜，没真正记住视觉映射。

**做法**（`tools/synth_atoms.py`）：
- 枚举全部单音符 token：7 数字 × 6 时值（裸/q/s/d/h/c）× 3 升降 × 5 八度 × 附点 + 休止 + x = **1479 个**
- 每个音符独立一张图：**拍号适配时值**（q3 用 1/8 拍、3. 用 3/8 拍）、**隐藏拍号显示**
- 标注 = token 本身 → 强制记忆
- 踩坑：safe 文件名 `,`→`d` 与三十二分时值 `d` 撞名（28 个被覆盖），改 `,`→`LOW` 修复

**训练**：原子 1479 + 整曲 600 = 2079 样本，2 epochs（520 步 ≈ 5.8h）

**验收**：`tools/eval_v7.py` = 原子抽样识别率 + 上海人/小星星/春天在哪里 GT 对比

## 三、真实图评估（唯一权威 = 用户手工 GT）

### 上海人（用户 GT，2026-08-24 修正低八度逗号后）

GT: `q1. s2 q3 q3 | q3. s1 q,6 q,6 | s,7 s,7 s,7 s,6 q,7. s1 s2 | 3 - | ...`

| 版本 | 同位置一致 | 低八度命中 | 备注 |
|---|---|---|---|
| v4 | 6/35 | 0/7 | 低八度全丢、q 幻觉 |
| v5 | 19/35 | 5/7 | 音高 35/35；s/q 混淆 10 处、附点丢 4 处 |
| **v6** | 22/35 | 7/7 全中 | s/q 混淆降至 ~5 处；新增 h/d 幻觉 3 处 |
| **v8** | **24/35（历史最佳）** | **7/7 全中** | 形态全覆盖训练生效：升降/附点/八度识别提升 |

### 小星星（用户 GT，2026-08-24）

| 版本 | q 幻觉 | 前 12 小节 | 歌词 |
|---|---|---|---|
| v4 | 17 处 | 部分对 | 混入 R{} |
| v5 | 1 处 | 全对 | L: 分离正确 |
| **v6** | **0 处** | **全对** | **L: 分离正确** |

### 其他真实图（v6，无 GT，肉眼可查）

- 听妈妈讲那过去的事情：低八度 `5,. q3`、`q2 q1. s6 q1`，歌词 L: 分离，结构完整
- 一字诗：`( ( 5 - 1 ) 1 | 5 - 1 ) 1 |`，五声音阶全对
- 明月逐人归 / 赋得古原草送别 / 亲人子弟兵 / Angel / 太湖水：全部输出完整谱面 + L: 歌词行，无 R{} 糊行

## 四、工具链

| 工具 | 用途 |
|---|---|
| `tools/synth_scores.py` | 随机合成谱面生成器（权重版，`[qsdhc]` 语义正确） |
| `tools/render_ly.py` | txt → .ly → PDF（`DATA` env；v3 起 beam 加粗） |
| `tools/pdf_to_png.py` | PDF → PNG + manifest（`DATA` env） |
| `tools/augment.py` | 扫描模拟增强（`DATA` env） |
| `tools/train_lora.py` | QLoRA 训练（`--manifest`、`--resume auto` 断点续训） |
| `tools/test_real.py` | 真实图推理（`LORA_PATH`、`REAL_OUT`；输出剥离歌词） |
| `tools/compare_lines.py` | GT vs 模型输出行级对比 |
| `tools/clean_out.py` | 歌词剥离后处理（中文/非指令字母词） |
| `tools/canon.py` | 音符 token 规范化（字符顺序无关） |
| `tools/gt_shanghail.py` | 上海人用户 GT（修正版） |

## 五、关键结论

1. **时值分布是幻觉的根因**：v4 训练数据 h(六十四分) 19.1% 而裸四分仅 13.3%，模型被教成"见音符先猜 q/s/h"；v5 修正后 q 幻觉 17→1
2. **下划线渲染粗细决定 s/q 区分**：jianpu-ly 默认 beam 0.1 极细，150dpi 下扫描噪声把 s 双线糊成 q 单线；v6 加粗到 0.4
3. **真实图永远是验证集**，域差距靠合成侧渲染真实度弥合，不拿真实图训练

## 六、产物

- 模型：`models/jianpu-lora`(v1) → `jianpu-lora-aug`(v2) → `jianpu-lora-v4` → `jianpu-lora-v5` → `jianpu-lora-v6`（当前最优）
- 数据：`train-data/`(v1-v2)、`train-data-v2/`(v5)、`train-data-v3/`(v6)
- 真实图输出：`train-work/real_eval_v4/`、`train-work/real_eval_v5/`、`train-work/real_eval_v6/`（v6 最新）、`train-work/gt/`（用户 GT 存档：小星星、上海人）

## 七、下一步

1. 批量转写全部真实图（test-jianpujia 100 张 / test-jianpucn 30 张）用 v6
2. 剩余问题：s/q 时值混淆（~5 处）、h/d 幻觉（3 处）——继续调合成渲染/分布
3. 用户标注 3-5 首真实图 GT（混训或仅验证）
4. 部署：LoRA 合并 → GGUF → Ollama，替换 convert.py 的 7b
